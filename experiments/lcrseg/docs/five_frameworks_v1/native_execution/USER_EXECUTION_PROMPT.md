# 给 Codex：真实接入 → 必要资格验证 → 自动启动五框架实验

> 将本文件交给 Codex 执行，即表示用户采用下面的连续执行授权。
> 这是新的执行授权，不是声称后续新增代码已获得外部审阅。原审阅记录不得改写。

## 1. 目标与授权

我授权你在已经提供访问权限的工作区、服务器、GPU 和数据范围内，完成真实接入、必要资格验证、当前域 L smoke，并在通过后直接启动既定五框架 B–D 实验。不要再以“等待新一轮外部代码审阅”为由在这些环节之间暂停。

仓库：DLwbm123/SSL_CL_seg
分支：codex/sslcl-five-frameworks-v1-review
已接受的模块提交：ab69b6b3cd7e0102cc929edfda8a0f4035f635dd
R01–R07：关闭并保留原回归覆盖。

本指令取代先前 prompt 中“真实接入后必须再次推送并等待外审才可训练”的工作流限制，以及 CODE_ONLY 对本次合法真实读取和训练的限制；不取消数据隔离、状态正确性、资源预算或既有研究设计。不要伪造 external reviewer、外部审阅证据或审阅 SHA。

## 2. 先确认真实父实现，不能用替代品直接跑

复用用户已经提供的父方法源码、配置、入口和工作区信息。一个用户明确认可的源码路径＋配置，或确定的 commit/入口即可绑定，不要求找回特定历史启动命令，不要求 SOTA 表，不重新做无界 Git/NAS 溯源。

父实现明确时，直接记录实际 A/B 乘法、参数白名单、单侧约束、阶段转移、native loss/optimizer/scheduler、几何与随机头语义。不要为了符合变量名称改变算法。

如果确实没有可定位、被用户认可的父实现，停止真实训练并只报告这一项最小缺失；不能自称找到了 KI，也不能用 toy、F_CONV、旧 SVD 或新写 O-LoRA 静默补位。多个不等价父版本且已有信息无法选择时，同样不得猜测。

## 3. 补完真实路径，而非只删运行拦截

实现并注册真实 runner；接通当前域 L/U 数据、各自源模型、阶段训练和恢复、单学生部署及隔离评价。禁止仅删除 synthetic 检查、修改注册表或将 parent_binding 字符串改为 BOUND 来冒充完成接入。

本次允许为合法初始化与训练读取必要的真实 source checkpoint，以及当前域允许的 L 图像/标签和 U 图像。U 隐藏标签、历史训练数据回放和测试标签调参仍然禁止。源模型谱系必须核验，不能把已经学习过后续域的模型当首域模型。

如需修改旧 gate，新增显式的 USER_DELEGATED_EXECUTION 路径：记录本指令、模块已审阅提交、新增生产提交、实际代码指纹、父绑定、计划、依赖及预算。既有外审路径保留；不编造“新增生产代码已被外部审阅”。

资格验证前冻结并记录实际执行源码；与训练无关的文档和报告可追加提交。涉及算法、数据或训练语义的修改必须记录为新版本，不能把不同版本结果混成一个候选。

## 4. 自动资格验证与 smoke

保留原 CPU 回归；在已授权设备上完成必要的 CUDA 合成检查，重点是 native 关闭新增功能的前向/损失一致性、CWMI 设备行为、五框架梯度权限、第二阶段恢复和部署。

沿用上一执行计划的资格范围：新增 CUDA 合成资格至多 256 次合成优化器调用；CPU 原回归及调试开销单列。当前 L smoke 至多 24 次实际优化器调用：F1=4、F2=8、F3=4、F4=4、F5=4。smoke 使用独立副本，不能继承进正式比较，不能依据其分割分数选配置。

只验证实现、数值、数据和恢复正确性，不要求 smoke 出现 Dice 增益。通过后自动继续，不再请求新的外部审阅。普通工程错误可在原定义内修复并重新验证，失败开销计入；超出明确资源上限、必须改变方法定义或无法解决的数据/数值错误时暂停相关任务并说明，不擅自无限重试。

## 5. 固定范围与预算

保持现有五框架独立比较；不叠加方法、不添加候选、不按早期性能取消某一框架。

- 两个序列：REFUGE→RIM→Drishti，REFUGE→Drishti→RIM。
- 4 组父配置；四类待搜索基线各 8 配置；F1–F5 各 8 配置。
- 每个框架选一个开发配置，全部五框架与五类基线进入三个新优化种子的复核。
- C–D 上限仍为 212 条轨迹／424 个目标阶段；合法别名去重只能减少任务，不补新搜索。
- 源模型每个指定优化种子最多新训练一个，默认四个源节点；合法复用必须匹配来源和种子。
- Phase E 仍不在本次授权内，不自动追加最终胜出框架的大消融。

实际预算按明确父配置日程展开：目标科学更新上限为 212×(S_RIM+S_DRISHTI)，源训练按至多4个对应源节点计数；不得擅自加 epoch、步骤、rank 搜索或新域。1,123,600 仅在 S_RIM=3200、S_DRISHTI=2100 时成立，不能硬编码成未知父方法的真实预算。

启动前自动生成并冻结实际有限预算，不为已有配方的机械预算展开再等待人工签字。F2 探针、F4 内层修正、F5 前向、资格、失败恢复、源训练与科学更新分账；不能只记录 optimizer steps 而忽略其他计算。使用既有资源权限和并发约束，不租用额外付费资源，不终止他人进程；既有资源不足时排队，不扩大资源范围。

## 6. 正式执行与选择

完成资格后，自动执行父/基线开发、五框架配置开发和全部五框架的新种子复核。严格使用各自源/阶段前驱，不交叉借用更好 checkpoint。保持最后学生与既定完整训练日程的评价定义。

依据已定义开发集指标选配置；复核阶段不调参。某个种子、顺序、类别为负、患者区间跨零或平均增益未达0.005，都不作为自动停止理由。也不挑选验证集上最好的 epoch 替换固定末尾学生。

最终报告全部候选及成本、Final/Old/Incoming、类别代价、种子差异与不确定性，选出最多两个后续研究候选。排名靠前不等于已经有足够证据称为论文成功主方法；没有稳定增量时如实说明。

## 7. 推送与真实启动回执

同分支追加提交并推送，禁止 force-push 或 main merge；不公开图像、患者身份、逐病例输出、checkpoint 权重、凭据或私有原始日志。公开代码、配置摘要、聚合结果、成本和必要运行回执即可。

生产代码推送后不等待外审；资格通过即启动。只有实际开始后才能报告 RUNNING，并提供真实 run_id、execution_commit、父绑定、预算、设备、进程/作业编号及第一条成功更新证据。仅创建进程但尚无成功更新时明确区分。

会话结束时只报告能够核实的完成/运行/阻塞状态；不得声称仍在无人监督地工作或将来必然自动回报，除非已有实际可用的执行与回报机制。保存可靠恢复入口。实验完成后推送聚合结果供用户交回审阅，不因为等待报告审阅而阻止已授权的固定 B–D 矩阵继续执行。
