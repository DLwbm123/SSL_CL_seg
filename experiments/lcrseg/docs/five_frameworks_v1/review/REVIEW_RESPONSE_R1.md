# External review R1 response — R2 submission

Status: **STOP_AWAITING_EXTERNAL_CODE_REVIEW**. External decision remains **CHANGES_REQUESTED**, not approved. Actual revision base and reviewed previous commit: `b909a9131dc7137232ac8eb3a6922fe66520b932`. Original study base: `8a6e93a8bc39554c878cffe0dedf7fbfefe7d16d`. Same branch, additive commit; no amend, force-push or main merge.

The supplied nine regressions were first executed against the fixed old commit in the existing torch 2.6.0 CPU environment: **1 passed, 8 failed**, matching the reviewer’s defect pattern despite their different torch 2.10.0 environment. See [local baseline](REGRESSION_BASELINE_R1_LOCAL.txt). The external input is preserved under `../external_review_R1/`; the original 104-pass report and code fingerprint are retained under [history](history/R0_TEST_REPORT.json) and in the immutable prior commit.

The final R2 qualification runs **162 tests: all passed, no skips or failures**. The three warnings are generated by explicit optimizer replacement in fault-injection tests; they are not ordinary trainer warnings. CUDA is NOT_RUN. See [actual report](TEST_REPORT_R2.json), [actual log](TEST_LOG_SUMMARY.txt) and [measured costs](CPU_COST_R2.json).

| Issue | Disposition | Implementation | Evidence |
|---|---|---|---|
| R01 | RESOLVED for the executable synthetic bridge; native KI binding remains separate | `configure_stage_training` restores only legal A/B flags and module modes, independent of `stage_entry`. `Model.for_resume` avoids eigensolve; `StageTrainer.for_resume` skips probes/source/prototype initialization, then optimizer and checkpoint state load. No nonzero-B reset or all-model training enable. | External R01 plus `test_stage2_independent_process_resume_complete_state` for all five frameworks and all five baselines. Student/EMA, flags, modes, F/Q/R, optimizer/scheduler/scaler, prototypes/support, probes, RNG, reads and multi-step trajectories compare exactly across processes. Original stage-1 comparisons remain green. |
| R02 | RESOLVED | `semantics.py` resolves all options including warmup/ramp, records family/rank, actual provider seed/order/stage/manifest/split/stream/source, actual entry tensor fingerprint, native metadata/free projector, code and backend identity, named optimizer groups and module/gradient flags. Save records the full object and digest; restore derives the actual object and rejects differences before loading trainer state. | External R02a/b; all loss coefficients, steps/warmup/ramp/lr, family/rank, different entry parent, provider seed/order/stage/manifest/split/source/size rejection tests. Nonsemantic output_path/log_every are separately permitted. No device/precision migration is approved. |
| R03 | RESOLVED | Active supervised/constraint/structure and combined L/U losses checked before backward. Optimizer/constraint/scheduler results, optimizer state, effective A/B and F, exact candidate EMA arithmetic and pending prototype cache checked before EMA/prototype/cursor commit. Failures require restore; physical optimizer invocation retained even for a partially failing call. | External inf-constant and inf-constraint tests; nonfinite optimizer value/state/effective matrix/scheduler tests; partial optimizer exception; skip and fault tails. EMA/prototypes/cursors remain uncommitted and recovery retains physical cost. No loss-decrease criterion added. |
| R04 | RESOLVED | `InsufficientProbeSupport`/`RankDeficientProbe` are the only caught categories. Malformed dimensions/rank, nonfinite gradients/projectors, invalid P and solver exceptions propagate. Candidate A values for all selected layers validate before any A write. Probe.complete stays false on engineering failure. | External P=2I; wrong shape, asymmetry, nonfinite P/G and no-A-mutation tests; existing no-support caller fallback and thin-SVD tests remain. Real locked CWMI parity tests are retained; toy structures only isolate exception behavior. |
| R05 | RESOLVED | Explicit model role/mode propagates into features and readout. Teacher prototype/U/F4 calls use teacher mode; student and deployment use student/eval. Frozen flags and module modes are checked separately. | External recorder plus behaviorally distinct teacher/student/eval native fixture across F1–F5, including prototype initialization, mixed student paths, U targets, repair readouts and deployment. Actual unknown stochastic native head remains unqualified. |
| R06 | RESOLVED | Dice intersects target!=255 with explicit valid mask. All-ignore returns no valid support and Dice=None. Partial ignore returns auxiliary distance/topology/area metrics as NOT_EVALUABLE requiring native evaluator; it does not fabricate anatomical boundaries along ignore edges. Fully valid image behavior is retained. | External ignored-pixel invariance; explicit masks, all-ignore, invalid shape/class checks; existing empty/one-empty fully-valid tests. No historical results recomputed. |

C01: **clarified, no new algorithm**. F2 direction probes remain `G_i = grad_W(lambda_structure * fixed_scale * CWMI_struct)`. Training L remains native CE+Dice plus weighted structure. No combined-gradient arm, extra grid or efficacy gate was added; original delivery text remains untouched.

C02: **resolved**. One full CPU FP64 channel eigendecomposition per new stage, then slice Q/eigenvalues and reuse the same spectrum for diagnostics. The dedicated resume model uses shape placeholders and loads saved Q/spectrum without decomposition. The old initialize=False compatibility route still accepts a caller-preconstructed Model; callers should use Model.for_resume to avoid constructing an unnecessary entry basis.

Additional CODE_ONLY integration work:

- `NativeParentBridge` now delegates actual named parameter groups and native feature/readout/loss/constraint/geometry/optimizer/stage hooks without importing a substitute KI. Synthetic callback tests verify native equality and reject an invalid multiplication contract. Native F2 adapter descriptors, source-specific sampler/state, optimizer and stochastic geometry still require original identity.
- `CurrentDomainDataAdapter` validates a reviewed current-domain manifest and has separate L and U reader capabilities. U schema rejects label fields; metadata tests verify no U-label call and cross-domain rejection. Real readers/manifest are not bound or opened.
- `ready_nodes` provides finite DAG readiness from sealed metadata dependencies. Actual CLI now routes through metadata preflight, recomputes commit/code/parent/plan/dependency bindings and validates all caps before invoking a registered native runner. The real runner registry is **empty**, so manual JSON edits cannot open a payload or start a run.
- CWMI now scopes the author's grid/mask tensor factories to each input device and caches separate pyramids by device. CPU pyramid outputs and gradients match the original backend. It no longer depends on hidden CPU-to-GPU mask generation; input tensors are not round-tripped. CUDA and mixed-precision execution remain **NOT_RUN**, requiring later explicit B authorization.

**Parent binding: PARENT_BINDING_REQUIRED. Real integration: PARTIAL_CODE_ONLY, no registered real runner or native isolated evaluator.** Minimum remaining clue is one original KI launch command or run_id. No new historical search or SOTA table request was made. No F_CONV, SVD or O-LoRA substitution was made.

The plan remains 4 parent settings, 8 configurations per searched baseline/family, all five families and five baselines entering three-seed replication: **212 trajectories / 424 target stages upper bound**, with actual source/target steps and effective configuration aliases pending original parent metadata. Phase E remains plan-only.

Real image reads, real label reads, real checkpoint tensor reads, real smoke and formal updates are all **0**. No GPU, server, source training, monitoring or B/C/D/E execution occurred. Green tests do not approve experiments; the external review lock remains unapproved.
