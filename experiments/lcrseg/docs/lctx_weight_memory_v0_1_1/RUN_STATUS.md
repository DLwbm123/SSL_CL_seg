# Recovery running: 399/420 gate passed

Started 2026-09-11T12:43:34.808779+08:00. Run `lctx_weight_memory_v0_1_1_20260911_01`; exact execution source `9dc9d6d5a44f113bfcabbcfef50f8ea7ee3075e5`. Detached parent PID 2145327, first GPU4 worker PID 2145361. Startup snapshot confirmed 159 committed formal optimizer updates, live neutral command lines, GPU worker memory1026MiB and no immediate failure. This is a startup snapshot, not a completed experiment or continuing monitor.

Local and original-server CUDA qualification passed: 29+29=58 new synthetic optimizer updates, zero real smoke. All18 original successes passed final-state re-audit with zero training, model forward and GT. All source and cached basis identities were reused; no new SVD/QR or source training.

The single full2100 rerun saves and compares update399, then saves exact420 before all-layer diagnostics. Only a passing420 evidence seal releases the17 unstarted tasks on GPU4/5/6/7, at most one task per GPU. The failed original420 remains separate cost. Remaining fixed work is49900 new formal updates in total; scientific95400, physical95820 if no further failures. Six STATIC_SOURCE evaluations follow all36 target seals. Actual errors stop new dispatch; no automatic retry or extra science.

NAS run root: `/data_nas/jiangsuiyang/LCR-Seg/SSL_CL_seg/protocols/lctx_weight_memory_v0_1_1_20260911_01`. Main log `parent.log`; task logs `logs/`; exact checkpoints and private outputs `tasks/`; all-layer review `reaudit/`. The completed report generator writes `public_results/FINAL_REPORT.md`, complete metric/cost tables and `TERMINAL.json`, then stops. GitHub final-result delivery remains pending until that work actually completes.

The original incomplete report and all scientific counterexamples remain immutable. Required class-single gates already have preserved negative examples; new averages cannot erase them.

The exact399 prefix comparison has now passed for student, EMA, Adam, counts, source boundary, data position and all399 step/loss records. Unseeded ambient process RNG states differ and are reported explicitly; the frozen keyed training streams and complete comparable training states match. Exact new420 was saved and all14 layer checks passed. The gate released the17-task queue. GPU4/5/6/7 workers are live, each with a neutral full command line verified through ps and nvidia-smi. This is still a running experiment, not a completed36-task report.
