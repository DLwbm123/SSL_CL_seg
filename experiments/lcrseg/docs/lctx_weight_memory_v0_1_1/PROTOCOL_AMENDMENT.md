# Diagnostic recovery amendment

The user-supplied EXECUTION_PLAN.md authorizes only diagnostics, exact-state archival, recovery and completion of the original matrix. The production FP32 D and W0+D, all fourteen layers, rank8, k, scaling1, initialization, Adam, EMA, LR, current labeled stream, LCTX objective, seeds61/62/63 and both orders are unchanged. Original reports, terminal, reservation, source weights and cached bases remain immutable.

The previous gate conflated parameterized D with the update recovered from a rounded FP32 weight sum. The new diagnostic keeps the D constraint at 1e-5 and explicitly amends the representation check to an independent addition budget plus an FP64 engineering envelope. It does not claim the old representation gate is unchanged.

One trainer is retained. Optional recovery instrumentation in that trainer saves post-update state before collecting all layer diagnostics and uses new admission. Evaluation arithmetic and scientific gate are retained; only dual-source resolution/reporting is added. No new SVD/QR is permitted on formal inputs.

18 old successes are re-audited without GT, model forward or optimizer updates. The old failed420 and saved399 remain intact. One new full2100 attempt includes prefix399 comparison and exact420 archival. The 17 unstarted tasks are released only by that engineering seal, never by scores. New49900 + reused45500 = scientific95400; old failed420 makes physical95820. New qualification <=120 synthetic; zero real smoke. STATIC_SOURCE follows the complete target-weight seal.

The pre-existing necessary class counterexamples remain: LR_SRC_A minus F_FULL, seed62/O1 old rim -0.07731558745756356; minus LR_RAND, seed61/O2 old cup -0.06875783410804343; minus LR_RAND, seed62/O1 old rim -0.07962406526873261. Completion cannot average away the -0.050 single-class condition. Original incomplete terminal remains separate from any new terminal.
