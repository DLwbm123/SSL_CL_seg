# DPR V0.1 running status

The fixed18-task matrix is actually running; this is launch evidence, not completed results. Run `dpr_v0_1_20260912_03` started at2026-09-12T14:56:06.243945+08:00 on GPUs4/5/6/7. Startup verified four neutral-command workers, finite committed updates and no immediate failure. The snapshot has134 committed updates; total admission is47,700 optimizer updates and supervised backwards, plus76,320 response VJPs. The only primary is DPR_U.

Execution source: `12ba9232204d5f183f48b67261a91efc0e4f6392`. The full math/Adam/EMA/recovery CPU and CUDA qualification each passed33 synthetic updates on83590c4b747264ed9e7d23eef19fce9127277565. Their training/evaluator source hashes are identical in the final execution commit. An additional zero-update check on both backends exercised all36 train/eval queue dispatches, sealing and the full paired-report/terminal path, including the final checkpoint hash binding. The admission receipts preserve both source identities.

105 synthetic updates are charged including all earlier attempts;12 real-L smoke updates were discarded. The initial local reference error used0 updates, a test-harness expected-exception failure used6, the earlier CPU qualification used33, and the first CUDA attempt rejected native adaptive-pool backward before its first optimizer step. Strict determinism remains enabled. The native pooling forward and exact adaptive bin derivative are preserved; local/native derivative checks and CUDA qualification passed after its deterministic VJP implementation. No formal failed-attempt updates occurred before this launch.

Measured real-smoke peak reserved memory is998,244,352bytes; admission is1,958MiB including headroom. All large runtime artifacts are on the verified NAS mount. Old preparation/qualification runs01 and02 remain preserved. No source, baseline or CIST training was repeated. Six real SRC_CE models,12 F_CONV/F_FULL evaluations and6 pre-existing STATIC_SOURCE references are bound through receipts/student/config/score identities. Both CIST negative terminals are unchanged.

The independent background queue finishes all18 trainings, seals every final weight, performs isolated final evaluation, writes the paired effects, class/domain costs, actual-step response diagnostics, resource accounting, final report and NAS archive receipt, then stops. It does not dispatch by validation score. No test/hidden/formal_03 access or automatic new experiment/monitoring is registered. Current EMA is diagnostic-only and follows the corrected update; U supplies only current-response VJPs.

Public launch evidence includes protocol, implementation, qualification, source/baseline binding and the initial startup snapshot. Actual final metrics are pending. Private images/GT, per-case results, weights, logs and raw NAS paths remain off GitHub. Background execution continues independently of this chat or SSH connection.
