# DPR V0.1 execution choices frozen before new data access

The attached plan is the authorized scientific contract. This implementation calls the immutable native F_CONV/AMS T_LCTX training step, intercepts its existing optimizer.step after the sole supervised backward, queries current responses before Adam, applies a FP64 small-system correction to actual FP32 Adam weight differences, and returns to native EMA. There is no alternate scientific loss loop. All three arms share this path and the438192-scalar14-layer whitelist.

Warmup logs g/d0 but never reads U, computes a VJP or copies parameters. The response API accepts only image and geometry; no GT mask enters pooling. Probe generator keys have no arm name and do not consume native streams. U batches use the existing unlabeled_order and unlabeled_geometry streams including the single-image tail.

Numerical admission separates FP64 algebra (1e-12 scaled tolerance) and FP32 applied state. For every target weight, the independent cast envelope is half the larger adjacent FP32 spacing, plus eps64 times abs(target64), plus2^-149. The progress budget is sum(abs(g)*coordinate envelope)+the FP64 allowance. No observed residual determines its own budget. Finite models, frozen subset, VJP gradient isolation, objective/correction bound and progress gates are mandatory. These are engineering gates only.

Diagnostics use the first step of epoch40 (global820 for O1 and1249 for O2), then every500 updates. These exact positions are enumerated per task. They reuse the current batch and original keyed LCTX mixing. Pooled32x32 class-contrast RMS and finite supervised loss differences describe actual applied raw/corrected weights. Scalar g/J first-order predictions are separately recorded. No nonlinear diagnostic score gates training.

Every step saves its pre-update recoverable state and then the post-correction/EMA pending checkpoint before acceptance. Pending holds the scalar row; only scheduled current-step diagnosis temporarily holds pre/raw14-layer snapshots. It stores no response J, image, feature, prototype or historical direction. Diagnosis precedes the next update and committed checkpoints discard these snapshots. Recovery of a pending nonlinear diagnosis deterministically reopens exactly that current batch; any additional physical accesses/forwards are counted separately. Raw Adam states are never valid resume points.

The CPU and fresh-process CUDA qualification each target33 synthetic Adam updates, plus zero-update formula checks. Real smoke has at most12 discarded L updates (three arms x two domains x two); U arms use stripped L solely for these bounded graph/memory checks, never for formal training. All formal U access follows the registered plan. Qualification failures/costs are retained. Full model count is2(student/current EMA), no extra diagnostic model. Separate parameter-vector snapshots, VJP rows, FP64 workspace and observed allocator peaks are reported. Initial admission uses1.25 times peak allocated/reserved plus768MiB.

Reuse six real source students and12 exact F_CONV/F_FULL baselines, with six already evaluated STATIC_SOURCE references for context. No source/baseline training or new STATIC_SOURCE forward. All18 final weights seal before any new validation evaluation. Only those final students are evaluated. No test/hidden/formal_03 access. Bootstrap shares patient weights across physical domains and all methods/seed/order with analysis seed2026091204.

The detached existing queue uses GPUs4/5/6/7 when measured free memory exceeds admission, never kills existing work, completes all18 tasks independently of scientific scores, seals, evaluates, writes reports to NAS and stops. A new create-only NAS run holds all runtime data. No auto monitoring or unregistered scientific work is created. Publication contains only approved public source/configuration/aggregate results; raw NAS paths and private artifacts are excluded.
