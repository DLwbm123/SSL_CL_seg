# PMGC V0.1 final closure

PMGC V0.1 is closed with scientific status `FAIL_PMGC_FEASIBILITY`. The uniquely resolved base is commit `06945fa738cf87a6cef22db949b627490e7847f1` on `codex/pmgc-v0-1-feasibility`; the GitHub branch API and `git ls-remote` returned the same full SHA.

The frozen gate decision is `G1=true`, `G2=true`, `G3=true`, `G4=false`, `G5=false`, `G6=true`, and `G7=true`. The failed gates are G4 and G5. The maximum current-class Dice drop is `0.00345029148374`; the maximum K2-mode CE worsening versus K1 is `0.036544748892`. K2-mode incremental value is therefore `NOT_SUPPORTED`.

The published PMGC evidence records all 48/48 formal pairs, 6654/6654 real forwards, and 464 passed tests with zero failures, errors, or skips. Model, checkpoint, bank, gradient, and ground-truth isolation passed. This closure changes no historical report, status, input, or private bundle.

The prototype-derived new-method line is `ENDED`. No additional PMGC variants, prototype ranking, prototype reliability, transport, or relation-loss work is authorized. The next protocol is the externally attributed `POS_MEO_JASCL_EXTERNAL_BASELINE`; POS and MEO are not contributions of this project. Its source audit, preregistration, and independent execution authorization remain separate prospective gates.
