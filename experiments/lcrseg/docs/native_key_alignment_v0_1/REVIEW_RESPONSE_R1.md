# External R1 response — code only

Base: `9915168cc3707fb70afdc2c719c8781cdf76e3fb`. External decision remains CHANGES_REQUESTED. This response is not a new approval.

## NKA-R1: canonical semantics

`protocol.canonical_plan` is pure construction; `freeze` is the separate writer. Hardcoded SHA-256 content anchors independently bind the published F5 results and frozen B2 plan. `validate_plan` compares the complete plan to that canonical expansion, including every node, four arms, options, prefix identity, analysis, diagnostic semantics and empty future phases. There is no aggregate-count substitution for per-node validation.

CPU rejects all five reproduced rehash examples plus PAS, ramp, projection, analysis, diagnostics, prefix identity and node ID (12 counterexamples), and both altered underlying anchor files. The separate existing cross-seed `validate_prefix` guard is preserved; additional phase/stage/arm refusals are tested. No evidence says the original delivered science plan was actually altered.

## A/B: admission and common prefixes

`authority.py` defines a separate sealed study capability and strict external-review/user-launch bindings: final commit, transitive code tree, scientific plan, prefix digest, execution definitions, exact D1 phase and separate caps. No approval/launch fixture is generated. The supplied actual R1 rejection cannot authorize anything.

`execution.py` adds a finite16-node D1 controller, scope-bound current provider and narrow prefix gate. Historical B2 receipt identity/digest are untouched. Real file/schema/finite/student/F validation uses shared integrity primitives, behind authorization. Direct construction also checks the canonical node, original receipt, expected prefix path and smoke scope before tensor IO. `validate_runtime` checks actual B2/native/sidecar/layer/protection/options/provider/loss binding before first L access. Four arms independently initialize mutable state from one bound prefix; no historical predecessor helper was relaxed.

## C: actual alignment resume

`state.py` reconstructs native adapters and **KeyAlignmentTrainer** directly, restores full native training state and exact saved auxiliary basis, and verifies study/arm/full options/layer/prefix/key/basis/namespaces/cost/diagnostics. No class monkeypatch and no oldStageTrainer resume. C2/C3 tests compare model, EMA, optimizer, scheduler, prototypes/support, cursor/reads, RNG, basis, counters and diagnostics after active continuous versus resumed updates. Both have nonzero auxiliary support/loss. Wrong arm/prefix/basis/lambda/diagnostics are rejected. C2 reconstruction is tested with basis generation patched to fail if called.

## D: accounting and persistent diagnostics

Shared Counter, NativeOperations, cost sessions and integrity primitives are reused; old authorization/DAG and scientific kernels are untouched. New checkpoints and receipts retain fixed-step gradient diagnostics plus aggregate alignment support/norms; support totals and clean-U/VJP counters persist across resume. Failure injection consumes its physical call, does not commit, cannot continue and cannot checkpoint. Unclosed costs and stale/corrupt integrity evidence are rejected by generated regressions. Future qualification definitions are separate: synthetic CUDA24, current-L-only smoke32, formal42400; no execution occurred.

## E: complete reporting

`reporting.py` requires16 rows, exact diagnostic coverage, current actual-integrity proofs and closed costs. It reuses the frozen macro-Dice/Forget definition; writes16 final,48 domain and21 paired rows, diagnostic/support and costs. C3-C0/C2/C1 pairing is seed/order → order mean within seed → seed mean. Per-domain rim/cup and early/final first-target scores are retained, and DeltaForget=-DeltaOld is checked. Generated report fixtures exercise completeness and missing-diagnostic refusal. No current scientific outcome is manufactured.

## CPU evidence and unchanged science

OldD0:30/32,2/2 untouched. CPU_INTEGRATION_R1 attempt1:17 calls,6/6PASS. Then tightened direct payload path/restore identity checks and persisted support diagnostics, reusing the existing metric validator; final attempt2:17 calls,6/6PASS. Supplement34/48,2/2; combined64/78. Two planned failure calls are included. Supplement diagnostics62 VJPs; total operation counters and both code bindings are preserved in COST_SUMMARY_R1 and attempt reports. Second attempt is the current code-bound PASS; no third attempt is authorized.

Science files D1_MATRIX/FROZEN_OPTIONS/PREFIX_BINDINGS/DIAGNOSTIC_STEPS are unchanged from base, including science digest `7e2fb72a08d2d985ca4497255ffe9d663592feebc78e9ac21c06968702efe2f7`. Method alignment kernel, historicalD0 evidence, F5 and shared modules are unchanged. Only validation/execution/resume/qualification/report/test definitions change the code manifest; separate EXECUTION_PLAN avoids rewriting scientific metadata.

Remaining: external code review, fresh launch authorization, actual four-prefix tensor acceptance, native CUDA, real smoke, formalD1. All are PENDING/NOT_RUN; no optimizer/patient/real-weight inference from CPU PASS. Status: **STOP_AWAITING_EXTERNAL_CODE_REVIEW**.
