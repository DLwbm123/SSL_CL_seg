# R1 review checklist and limitations

- [x] Exact canonical plan and pinned underlying evidence; rehashed counterexamples rejected.
- [x] Original science/options/prefixes/diagnostic steps unchanged; original D0 and F5 retained.
- [x] New study authorization and finite D1 controller implemented; no approval shipped.
- [x] Actual native B2 objects, options, provider and prefix paths bound before authorized payload paths.
- [x] KeyAlignmentTrainer full continuation, exact C2 basis restore, C3 V match and negative restore tests.
- [x] Fixed-step diagnostics/support persisted; physical failures and separate costs retained.
- [x] Complete16-node report fixtures with common-prefix pair ordering and Forget identity.
- [x] Supplemental CPU6/6PASS,34 calls/2 attempts; combined64 calls.
- [ ] New external code approval and independent user launch confirmation.
- [ ] Actual prefix tensor acceptance (all four).
- [ ] Native384 CUDA qualification24 calls and correctness.
- [ ] Real L-only smoke32 calls, formal warmup and discard.
- [ ] Formal D1 and results/integrity/publication.

CPU uses native architecture with32×32 generated inputs, foreground fixture and PAS0/-1 only for qualification. It does not prove384 CUDA memory/throughput/correctness. No dependency upgrades or optimized Python were used. Native V/SWD/mask science is unchanged; no claim of independent-patient generalization, complete new-method trajectories or key-specific advantage follows from code tests.

Formal runtime reuses the existing single-update engine and data semantics. Future CUDA failure, environment mismatch, missing prefix or loss of cost/checkpoint tail stops; remaining numeric budget does not authorize retries. CUDA/OOM/source/provider execution is still a review-and-qualification risk, not a passed test.

Stage resume uses the shared strict restore after constructing the correct trainer; it reads the generated/native checkpoint twice, preserving existing validation rather than duplicating it. This IO is part of the session wall cost. Operation counters cover model/gradient/optimizer work and can overlap semantic telemetry; diagnostic subsets must not be added again.
