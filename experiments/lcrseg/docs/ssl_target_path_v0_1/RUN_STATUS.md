# SSL_TARGET_PATH_V0_1 — running

This is verified launch evidence, not a completion claim. Execution source: `098c9c73dc1b7dd1b723363e45cbe34e169fe354`. Base: `60843e85c110f31863299639822b080cf4c4ba9b`. Branch: `codex/ssl-teacher-target-path-v0-1`.

Exact-source CPU/CUDA qualifications passed5 targeted tests each. All4 qualification attempts total616 synthetic optimizer updates (cap1000). Isolated real smoke completed10 updates (cap10), using only the first2 sorted labeled cases per domain with labels stripped for pseudo-U. No real U images or val GT were read for smoke. The superseded metadata-only preflight and prior CPU source evidence remain preserved.

The complete30-task matrix is admitted before any formal result: seeds31/32/33 × RIM_ONE_r3/Drishti_GS × SUP/J_MSE/T_MSE/J_SCE/T_SCE. All tasks execute100 epochs independently, including independent warm-up. Total formal budget79500; historical total if all complete248108. T_SCE remains the sole main candidate. No seed/recipe admission depends on early val.

Detached parent PID `74345` has PPID1 and its own session. The first startup check observed1514 real formal updates across3 active tasks, no failure records. GPUs4/5/6 are used; each task requires at least4GiB free. GPU7 was below that requirement at planning. Other processes remain untouched.

Output root: `/data_nas/jiangsuiyang/LCR-Seg/SSL_CL_seg/protocols/ssl_target_path_v0_1_20260908_02`.

Parent log: `/data_nas/jiangsuiyang/LCR-Seg/SSL_CL_seg/protocols/ssl_target_path_v0_1_20260908_02/runner.log`. Per-task logs: `seed<seed>/<domain>/<arm>_train.log`; per-step records/checkpoints: `seed<seed>/<domain>/<arm>/`.

The queue runs independently of SSH/Codex and stops after all registered tasks and fixed result compilation. No recurring monitor was created. Final validation, full interpretation, public result delivery and NAS publication archive remain pending until the queue completes. Data, GT, patient identifiers, per-case arrays and weights stay on NAS; only source, protocol and aggregate operational evidence are public.
