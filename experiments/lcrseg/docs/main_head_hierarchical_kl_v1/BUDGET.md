# Separate preparation and proposed execution budgets

Current authorization: generated CPU only, at most two attempts of 16 Adam invocations (32 total); planned 15 per successful suite: C0 equivalence 4, C1 continuation 5, C2 continuation 5, predefined after-optimizer failure 1. Every call is charged before invocation. Autograd.grad cap 128 per attempt, separately logged. No extra retry beyond these two preparation attempts; preserve all failures. New true data/model/GPU execution remains zero.

Future proposal only: CUDA 15 generated calls with the same 4+5+5+1 split, smoke 4 per arm/order =16, formal 4 nodes =10600, real total10616; no additional VJP (cached parent/conditional/weight summaries at 16 fixed points). Failed qualification is included and blocks progression; no automatic qualification/formal retry. C0 historical import is conditional on native no-op equivalence and all original bindings, not granted by synthetic checks. A separately authorized C0 rerun adds5300, yielding15900 formal; it is excluded from the current planned real total. P2/C3/new-seed work is not authorized or implemented.

P0-B has zero optimizer but nonzero private reads and forwards. Four states; L one fixed full pass each, U four fixed batches each. Actual manifest cardinality and batch/image IDs are unavailable without separately authorized metadata access, so exact forward count is deliberately UNBOUND and P0-B cannot launch. Freeze these counts plus model-role (student vs dense EMA), prototype reconstruction, preprocessing, and immutable checkpoint receipts before requesting payload access. Deployed B2 student is not silently treated as historical EMA. No missing state may be recreated by training.
