# External review request: AGMS_DS_HALF_V1

Status: **STOP_AWAITING_EXTERNAL_CODE_REVIEW**. No new production approval is present or inferred. Current user's autonomous research delegation is recorded in EXPERIMENT_PLAN; it does not replace genuine independent review required by the production validator.

Base approved science/implementation:89dc7657c4f0b7c6f51de339d0f6bc2a621da55f. Full prior results:ef6888dc81519ce9e9ca13ea497bbf6397747d70. Current branch:codex/agms-ds-half-v1; review its exact HEAD reported by the submitting agent. Do not substitute the old approved commit.

## Scientific diff

Only A5 lambda_DS .25→.125. Same B2 prefixes, seed163, two orders, second target, native A-only model, frozen linear head, PAS, H, risk formula/lag, EMA, current data exposure, LCTX collection and original auxiliary-head RNG namespace. Head lr stays.001. No added mechanism. Lowering DS changes both native gradients and head learning; do not market it as a pure gradient-routing test.

Two new nodes,5300 formal updates. Original A0 and A5 endpoints are imported by pinned aggregate row/proof hashes. Proposal/gates are specified before any new real result. The scientific hypothesis is based on completed single-seed development evidence; no independent significance/generalization claim.

## Code diff and focus

This isolated branch specializes the existing package and keeps one training loop. Original approved execution checkout and old docs, CPU evidence, reviews and results are unchanged. Inspect:

- protocol.py: independent study, two canonical nodes, new caps and exact prior-control binding; no original future P1R/P2/P3 nodes.
- trainer.py: all three DS consumers (loss, weighted telemetry, diagnostic norm) use frozen METHOD.lambda_DS consistently. No other gradient computation changes.
- authority.py: real external approval still mandatory; fresh commit/science/prefix/both-control imports/environment/execution and current user receipt bound, old approvals rejected.
- execution.py/qualification.py: finite budgets, order-specific L-only smoke, imported P0, original full prefix/model verification and no-replay rules.
- reporting.py: matched A0/original A5 imports, six endpoints/eighteen domains/nine contrasts/eight new diagnostics, costs and gates only after both nodes.
- state.py/model.py/native_data.py/training loop unchanged: semantic record includes new study and DS method; wrong-dose/old-study restore rejected.

## Validation performed

One independent native synthetic CPU attempt **PASS21 calls** (hard cap24). No old CPU suite rerun; oldAGMS80 and earlier134 remain separate. Nine check groups pass:scope, native gradients/EMA/U whitelist/A-only/deployment, dose consistency, exact checkpoint continuation, invalid identity/dose/prefix rejection, report coverage and actual generated-model integrity, off/B2 equivalence, expected failure accounting, two fixed-batch fitting cases. Both expected after-optimizer failures are included in21; no unexpected failures. Evidence:CPU/TEST_REPORT.json,ATTEMPTS.json,PHYSICAL.jsonl andcosts. Generated tensors/checkpoints stay private on NAS.

Future CUDA11, real prefix verification, smoke8 and formal5300 are **PENDING**, not claimed passed. Real optimizer calls for this new study are **0**. No approval template is shipped. Review approval, if actually granted, must use the exact fields and phases documented in RUNBOOK. Do not rewrite prior R2 or label this independent AI review a human signature.
