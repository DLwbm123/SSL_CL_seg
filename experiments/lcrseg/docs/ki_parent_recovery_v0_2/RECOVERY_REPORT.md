# KI Parent Recovery V0.2

**PARENT_NOT_LOCATED_IN_ACCESSIBLE_SCOPE.** No original A-only KI source/config/run identity chain was established in the bounded accessible project scope. This is not a claim that KI does not exist or that the method is ineffective. The blocker is implementation identity and behavior-defining configuration, not missing historical scores and not GPU permission. G1 is not admitted; no training or synthetic qualification was launched.

## What changed from G0

[PROTOCOL_AMENDMENT.md](PROTOCOL_AMENDMENT.md) explicitly separates implementation identity from historical performance. If the implementation and effective configuration had been verified and only old scores were unavailable, new matched KI_REF training would have been allowed after qualification. That condition was not reached. `implementation_identity_verified=false`; `historical_performance_verified=false`; compatibility and D0 remain unknown, not failed performance measurements. Existing G0 files and its BLOCKED terminal are unchanged.

## Completed recovery

- Reused the previous39-tip audit rather than repeating it. Inspected224 reachable/reflog commits by deduplicated object content:1103 source/config/shell/Markdown blobs and171 targeted JSON metadata blobs. Local/server stash and tag lists were empty; all9 distinct server reflog commits were already present locally. Eight oversized JSON metadata blobs were excluded from content reads.
- Read1187 selected runtime metadata texts across the authorized project roots in two bounded passes (622+565), including288 log headers (22+266). Inspected42 actual effective config.yaml files and42 command.txt entrypoints; these bind existing LCR/SR-GAS variants to their runners. This is stronger evidence than folder names, but none identifies A-only KI. First-pass bytes read1729613; no invented aggregate byte/open count for other manual follow-ups.
- Followed12 explicit Python source references from runtime metadata. They resolve to the existing LCR/PRES/SHOR/single-teacher source families, not a newly found KI implementation. Raw runtime path/reference mappings remain in the private NAS audit; they are not copied to public CSVs.
- Indexed three prechange source archives:91/437/515 members, reading only83/157/201 allowed source/config members. Their only mechanism-like hit was the `low_rank` variable for a percentile-order unit test, not low-rank adaptation. No archive was expanded, no data or checkpoint tensor read.
- Inspected three project-present third-party checkout identities and README headers: JASCL `3c93ca70784fc3a1d2a887f8d7dce5af6bc75f53`, LAG `3dbd3f8dcee06770aa9c4412f447147b2520c71f`, STAR `6c9203aa0c91e9a2d4e40664c97754ae02226675`. They identify other methods and supply no original KI run binding. New runtime repository references were the current project, JASCL and a PyTorch dependency reference; no evidence justified searching unrelated user repositories.

## Excluded leads and semantic checks

[CANDIDATE_LINEAGE.csv](CANDIDATE_LINEAGE.csv) records rejected discovery leads; these are not accepted KI parent candidates. Its source_commit column identifies inspected source snapshots or third-party HEADs; it does not assert those commits produced an original KI run. SR-GAS A6 genuinely has a frozen-body control, but `srgas_v0_1.py` enables the dense classifier weight, not an A-only low-rank subspace. The orthogonal Gate1B object is a16-dimensional Procrustes feature transform `R=U@Vh`, not a segmentation-network parameter update. Other adapter hits refer to manifest interfaces or explicit prohibitions. The recent KI plan/audit is not retrospective implementation evidence. The known full-parameter MIX runner remains an ineligible substitute.

No source commit, A/B dimensions, rank, D0 identity, effective schedule, or formal budget was guessed. A-only would constrain the specified actual subspace and would not by itself imply freezing B; however no accepted implementation was found on which to establish those semantics. Neither a new KI port nor an O-LoRA replacement was implemented.

## Coverage and failures

Scope: the known local SSL_CL_seg Git repository/worktrees and reachable/reflected history; project ignored third-party source indexes; server project runtime metadata, referenced code, known source archives and relevant Git worktree metadata. Runtime discovery limited traversal depth to3, with a final depth2 pass for project-root/method output metadata. Text selection excluded formal_03, hidden/evaluation/per-patient/per-case/prediction/data directories, credentials and binary artifacts; log reads were first8192 bytes, selected metadata capped at262144 bytes. Historical source text cap524288 bytes.

An initial unbounded filename-only directory walk took over110 seconds and was terminated as this task's own inventory process. Its partial filename count is unknown; it read no file contents. A bounded replacement completed. An initial Git path-filtered object listing exposed too few named blobs; it was corrected by enumerating object-name metadata and filtering eligible paths before reading blob contents. No incomplete search was counted as complete. A convenience source lookup used the older main checkout and missed a newer foundation file; subsequent interpretation uses exact historical blob identities and the existing integration source, not that missing path.

Uncovered: other users/projects; unreferenced external/private repositories; inaccessible or expired/unreachable history without a reference; excluded deep runtime/private data; forbidden formal_03/hidden/test GT. These are not recorded as confirmed absent. All explicit relevant references checked in the final round yielded no new KI source/config/run lead, meeting V0.2's bounded-stop rule.

## Required next clue and actual execution

The minimal missing lead is **one original KI launch command or original KI run_id** (from the original implementation work, not the later plans). That would let recovery follow a concrete source/config binding. A historical SOTA table is not required to unblock implementation recovery, and asking again for a repository/config/results three-item bundle is unnecessary.

Actual model forwards, backward, optimizer, EMA, GPU jobs, synthetic/smoke updates, image/GT/checkpoint-tensor reads: all0 in this recovery. Historical operation counts remain unknown where not audited. No valid TASK_MATRIX/BUDGET or PASS test evidence was generated without qualification. GPU4/5/6/7 sharing authorization remains valid and will be checked live only at actual admission. The old106000 updates and VALUE_REPRODUCED are untouched; no G2/G3 or substitute experiment started. Source/config/publication diagnostics used existing tooling; no new training implementation or dependency was added.

The five required recovery records plus the supplied protocol are published on `codex/ki-parent-recovery-v0-2`. Public files contain only source-relative references, method identities and sanitized counts/reasons. Private runtime metadata and raw path mappings remain on NAS. See PUBLICATION_VERIFICATION.json for verified commit/access status after publication.
