# KI × Anchored Mix V0.1 G0 result

**BLOCKED_KI_PARENT_UNVERIFIED — G1 not launched; all new optimizer updates0.**

The authorized G0 audit could not bind the required original A-only KI implementation to its configuration and verified formal results within the inspected SSL_CL_seg branches/server project. GPU4/5/6/7 sharing is authorized and GPU permission is not the blocker. Protocol section2.1 requires this terminal when a valid parent cannot be found; it forbids inventing a replacement KI implementation.

Required input to resume: the original KI repository/commit or source path, its exact configuration and corresponding formal-result directory. Those must resolve A/B shapes/order/rank, frozen/trainable parameters, domain-boundary state, optimizer and per-domain budgets, deployment semantics and lawful D0 lineage. Unresolved fields and the complete bounded-search evidence are in [KI_PARENT_FREEZE.json](KI_PARENT_FREEZE.json) and [COMPATIBILITY_REPORT.md](COMPATIBILITY_REPORT.md).

The current full-parameter Anchored Mix runner cannot be reused as KI_REF. Actual arm count, D0, schedule mapping and formal budget remain unknown; [TASK_MATRIX.csv](TASK_MATRIX.csv) has no admitted tasks and [BUDGET.json](BUDGET.json) does not invent numerical budgets. No new validation/test/hidden GT or checkpoint tensors were read. No training or synthetic qualification was run against a substitute method.

This is an input/engineering blocker, not a negative experiment result. The prior Anchored Mix VALUE_REPRODUCED and all106000 prior updates remain unchanged and are not recounted. G2/G3 have not started. Audit stopped after documenting the missing parent; a supplied parent location can resume G0 under the existing execution authorization.
