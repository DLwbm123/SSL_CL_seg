# Source diff and checks

New branch `codex/ki-ams-v0-1` starts at the published predecessor receipt `b499b073833bb3721c7167b094b193edfc114414`; this is an audit/documentation base, not the missing KI source parent. Only `experiments/lcrseg/docs/ki_ams_v0_1/` is added. No model, training, evaluation or historical source was edited; no experimental implementation was invented.

Completed: full attachment read; origin fetch and remote-head inventory; static source/config/commit-subject searches; canonical server source and run-name inventory; live GPU snapshot; structured report parse and header-only task-matrix validation; scoped git diff whitespace check.

Not run: restore-KI_REF, source-map/odd-U/no-target-gradient, checkpoint-resume and deployment qualifications. They depend on an identified KI runner. Existing Anchored Mix qualification is historical evidence only and is not passed off as this integration's qualification. No runtime dependency was installed.

A failed convenience lookup for `ssl_anchored_mix_v0_1/train.py`, `protocol.py` and a guessed reference `networks/unet.py` was corrected by inspecting actual engine/core filenames; no missing-path error was interpreted as a scientific test. The canonical source-only checkout's git-status failure was handled as deployment metadata, not as loss of experiment evidence.

The task CSV intentionally contains zero data rows. Null budget/parent fields mean unknown, not zero-cost admission. `KI_PARENT_FREEZE.json` status prevents any claim that G0 compatibility or G1 training passed.
