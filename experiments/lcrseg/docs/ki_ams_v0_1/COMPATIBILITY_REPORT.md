# G0 compatibility audit

**BLOCKED_KI_PARENT_UNVERIFIED.** User instruction to execute on GPU4/5/6/7 supplies execution authorization for G0 and gated G1. The attachment's draft/no-server-authorization boilerplate is not treated as overriding that request. G2/G3 remain excluded. The blocker is missing parent evidence, not missing GPU permission.

Fetched origin, listed remote heads and searched 39 distinct local/remote branch-tip commits for exact KI / A-only / key-isolation identifiers and LoRA A/B or class implementation identifiers in tracked Python/YAML/Markdown, excluding formal_03. No matches. Commit-subject search for isolation/A-only/LoRA/orthogonal did not identify a KI method parent; incidental MMPR isolation and Gate1A-only entries are unrelated. This is a scoped search, not proof that KI cannot exist elsewhere or in an unnamed historical/untracked version.

Server: searched Python/YAML code under `/home/jiangsuiyang/SSL_CL/code`, excluding formal_03, reports and docs, with the same identifiers: no matches. Also searched the canonical source-only `code/SSL_CL_seg/experiments` Python/YAML/Markdown tree excluding formal_03 and report tables: no KI matches. That canonical directory is source-only (git status returned not-a-repository); later experiment worktrees have separate git metadata. NAS protocol/run directory-name inventory identified existing SSL/LCR/GAS/routing/regularization runs but no parent binding for KI; no historical checkpoint or dataset tensor was opened.

The available Anchored Mix source is a full-parameter single-domain runner: `ssl_anchored_mix_v0_1/core.py:11` delegates model construction to the raw-linear predecessor, `engine.py:15` takes a single domain and defaults to100 epochs; its optimizer comes from `single_teacher_scd_v0_1/engine.py:206`, selecting all parameters with requires_grad. The raw head only freezes inert sigma/grad_update (`ssl_head_control_v0_1/core.py:20`). There is no verified KI A/B update mechanism or KI domain transition to preserve. Existing LCR/GAS or generic continual runners cannot supply that identity merely because they perform continual training.

No existing formal result is designated as KI_REF, no D0 domain is guessed, and no old source/checkpoint is substituted. See KI_PARENT_FREEZE.json for null unverified fields. No synthetic KI qualification can honestly be marked PASS without the parent implementation. No G1 reservation or jobs were created.

GPU snapshot at audit: GPU4 free7343MiB, GPU5 free16654MiB, GPU6 free16664MiB, GPU7 free16666MiB, with other processes active. These are historical observations, not a launch reservation or verified KI peak-memory admission. Recheck free memory once before any eventual launch; sharing is authorized without exclusive access.
