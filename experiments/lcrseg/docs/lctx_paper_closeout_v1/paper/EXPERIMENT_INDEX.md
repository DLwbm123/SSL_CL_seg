# Experiment index and selection timeline

`EXPERIMENT_INDEX.csv` lists all 30 new targets plus six reused source and six reused standard-label LCTX entries. Every new target's initialization is its own order/seed SRC_CE student from source commit `87bb70ee05609e77935b1bab5b9a44c9532dc491`, published with results at `e2ac3943a61447572489b8772dee71cedaf65d6b`. Runtime source is `3d9dade9ab254b4e3cabb488f3034e3198a67087`, bound by the new reservation and launch receipts. Run status is a dated startup snapshot, not completion.

1. Historical full-parameter AMS study (seeds 61–63): LCTX positive average developer finding; published report `671d0e57`. Preserve its original terminal and guarded decisions.
2. DPR/FINITE historical primary remains `NO_PRIMARY_ACCURACY_GAIN`; its SIGN control is exploratory.
3. SIGN saved-score old-seed analysis and frozen seeds 71–73 replication are separate. New replication terminal is `NO_POSITIVE_REPLICATION_ESTIMATE`, reported at `e2ac3943`.
4. After those observations, the user selected labeled-context recipe attribution and target-label sensitivity. Thus the topic selection is development-informed, even though all new arms, seeds, low-label subsets and analyses are frozen before new evaluation.
5. The new package uses F_CONV (438,192 scalars); it is not original KI. Main comparisons stay within a common source/parameter/target-label setting. Historical full-parameter and single-domain SSL results are separate context, not entries in a pooled ranking.

Two stage orders are paired within seed, not six independent optimization seeds. All six reused sources and six LCTX targets incur zero new training and zero new evaluation in this package. Existing patient scores supply source-stage and LCTX evaluation terms. The 30 new targets receive one fixed final-student evaluation each after all target weights seal.
