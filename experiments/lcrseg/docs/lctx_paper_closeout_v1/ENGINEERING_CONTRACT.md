# Paper convergence execution contract

The user's 2026-09-13 request authorizes the attached plan's fixed experiment and writing package. `USER_PLAN.md` preserves that specification, whose original PROPOSED status is historical; it is not a completion receipt. `PROTOCOL.json` and `TASK_MATRIX.csv` freeze both blocks before any new evaluation. Historical sources, methods and terminal states are unchanged. Code reuses the existing target loop, native LCTX input generator, Adam/EMA update and final-student scorer in a new namespace.

New work is 30 targets and 79,500 updates: 18 standard-label targets (47,700) and 12 low-target-label targets (31,800). Sources and six standard-L LCTX targets from SIGN replication are reused. Their existing source/deployment metadata are bound, without rehashing all old models or retraining their prefixes. Each new target loads only its own order/seed SRC_CE final student and resets Adam and current EMA. Fourteen named convolution weight tensors (438,192 scalars) train; other state is frozen. Existing architecture, head, padding, output resizing, optimizer, random keys and poly schedule remain unchanged.

The FULLMIX implementation calls the original LCTX step and changes only source collection/scoring inside that call. Thus donor reversal/noise, masks, image forwards, numerical loss/update path and EMA are shared. At epochs 1–20 the original clean CE+Dice executes. Thereafter, one `supervised_parts` call sees concatenated log probabilities and complementary integer mixed labels. CE divides by all valid pixels; Dice averages foreground classes within each synthetic image then averages images, including the existing all-ignore-image handling. Raw source multiplicity is two. Without ignore pixels, doubling valid occurrences and denominator gives coefficient 1/2 per occurrence. It does not double the summed gradient. Ignore pixels and nonlinear Dice prevent an exact per-pixel/objective equivalence claim. No extra clean loss or reconstructed-source loss is added. C_CE/C_CED call native clean recipes throughout.

Low-L manifests use only existing labeled-patient metadata, select floor-half (minimum two) by the specified SHA256 key, and retain identities on NAS. The public subset receipt contains the requested manifest hashes. Filtering happens before any HDF5 sample access; source, U and evaluation loaders are denied during training. Native sample-file integrity checks remain in place. New model hashes are retained where the inherited checkpoint/provenance contract uses them, not added as a transfer verification pass. Per-update fixed-state hashing is replaced by finite-state/gradient checks and the final fixed-state check.

Drishti L5 has an odd final batch: its last shuffled patient is paired with the first of the same shuffle cycle, who is necessarily different. Existing shuffle/geometry keys are unchanged, both low-L arms share this deterministic rule, and actual min/max/mean exposure is reported. Formal steps remain 21 or 32 per epoch regardless of L size. Standard L16/L10 retains the exact old batching.

The existing loop saves pre-update and post-update complete student/EMA/Adam/RNG/position/count state before diagnostics. It protects completed tasks. Qualification exercises mid-run and final-pending resume. Production never automatically retries: a failed attempt and physical operation events remain intact, and any recovery must reconcile durable scientific state with extra physical work first. No optimizer-state reset disguised as resume is allowed.

Training admits current L only and forbids response VJPs. Finite loss/gradients/weights, legal source identity, frozen parameters, complete-state recovery and truthful reporting are validity conditions. Effect size, CI crossing zero, unfavorable seeds/classes and missing noncritical diagnostic aliases are not admission gates. All final target weights seal before the 30 new evaluation processes start. Six existing LCTX/source scores are reused, not rescored or reselected.

Qualification is capped at 128 synthetic updates plus 12 discarded real-L updates, counting failed attempts. Shared GPUs 4/5/6/7 are admitted using actual smoke memory plus headroom, without stopping unrelated processes. A finite detached queue produces paired results, operation costs, an NAS receipt and updated Markdown draft tables, then exits. No monitors, seed additions, tuning, third-domain training or main merge are created. Public GitHub closeout occurs after actual completion and verification; private identities, data, weights and raw scores stay on NAS.
