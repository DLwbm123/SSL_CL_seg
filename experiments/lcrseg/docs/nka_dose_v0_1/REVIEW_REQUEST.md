# External code review request — NKA_DOSE_V0_1

**STOP_AWAITING_EXTERNAL_CODE_REVIEW.** Review the final commit on `codex/nka-dose-v0-1`; this request is not approval. The full source tree digest is in CODE_MANIFEST.json and the final CPU report. No production approval or user launch receipt was issued.

## Boundaries and source evidence

The branch starts at `145b3c1b3ab301031ea379c9d67452e57b28ae1d`. Existing Python sources, NKA/F5 plans, approvals, ledgers and results remain byte-for-byte protected by the original manifest. New code and review material are limited to `experiments/lcrseg/nka_dose_v0_1/` and `experiments/lcrseg/docs/nka_dose_v0_1/`.

Aggregates and completion/environment metadata are copied from `0da596f4050a3701f41ff74248ca9d4ba21222e2`; the four original B2 stage1 prefixes retain their bindings from `3034b2199aa7d6e54ea67499f391a0dd4ea3d21e`. INPUT_ANCHORS.json fixes the exact imported files. Protocol validation rebuilds all new nodes, old result/proof identities, options and environment from these fixed sources. Historical tensor acceptance remains PENDING; metadata checks do not claim that model tensors were inspected.

## Implemented change

DoseTrainer directly reuses the existing StageTrainer update, original alignment kernel and auxiliary random namespace. The narrow loss adapter owns explicit lambda, separate study capability and dose checkpoint identity. Old exact-type and 0.05 gates are unchanged. New production needs a fresh external review, separate launch receipt, clean checkout and all exact digests. Only C2/C3 at 0.5/2.0 appear in the formal matrix; 0/0.05 are generated-test-only allowances.

The 16 new nodes are C2/C3 × 0.5/2.0 × seeds163/164 × O1/O2, all stage2 from their own B2 stage1 prefix. RIM3200/Drishti2100 gives 42,400 updates. No source/stage1 updates. Twelve C0/C2/C3 historical rows retain original identities and are never retrained. No high-dose C1, old endpoint warm-start, new seed, D2/D3 or performance pruning exists.

The read-only meter adds four VJPs on the exact S, weighted KL, weighted alignment and actual U scalar objects before the unchanged engine gradients. It computes two clone-owned Adam candidates, preserving None semantics and PyTorch2.2.1 single/foreach operation order. It never invokes a second optimizer update or assigns actual gradients. Complete effective weights include the A/B cross term. Full/upstream/per-layer/A/B scopes report numerator, denominator, resolution, null reasons and actual increments. A/B-only W scopes hold the other factor at entry and are not additive. Actual assisted candidate parameters, increments and moments are checked after the sole real update, before publishing new diagnostic/checkpoint evidence. A mismatch marks the trainer unusable and stops execution.

Production cost sessions record candidate arithmetic, effective-W evaluations/time and four extra VJPs separately; operation counters overlap semantic subsets. Formula timing is nested within meter/session timing and must not be summed twice. Checkpoints preserve dose, lambda, basis/V, full optimizer/RNG/options/prefix, meter version, support and committed diagnostics. Separate sessions preserve failures and forbid lost-tail replay. Target acceptance and sealed skips inspect actual artifacts using the inherited integrity primitive.

## CPU evidence and repairs

| Attempt | Declared cap | Actual calls | Outcome |
|---|---:|---:|---|
| 1 | 29 | 12 | FAIL: snapshot time-field filter treated integer Adam state keys as strings |
| 2 | 29 | 29 | PASS after correcting the filter |
| 3 | 29 | 29 | PASS on final tree after removing misleading legacy arm-level 0.05 metadata, adding equal initial-state/basis checks, and enforcing external approval storage |

New total **70/96 calls**, **3/3 attempts**, including three planned after_optimizer physical failure calls. Old CPU remains **64**, combined bookkeeping **134**. The remaining 26-call allowance does not authorize a fourth attempt. All failed evidence and every physical ledger row remain present. No CUDA initialization, real checkpoint/patient access or production updates occurred.

The final suite verifies 12 rehashed canonical counterexamples, changed historical/environment inputs, old capability refusal, pre-payload production refusal, exact old/new 0.05 and meter-on/off common state, dose-independent initial state/RNG/basis, native continuous/resumed C2/C3, all four high-dose cells, wrong dose/prefix/basis/meter restore rejection, Adam None/zero/uninitialized/per-parameter-step/small-lr oracles, effective-W cross term, null ratios, failed/tail/sealed protection, inherited generated integrity/cost regressions, and 28/84/63/64 report schema. CPU input32 and fixture PAS0/-1 are generated-only; production stays384 and PAS.6/.7. Adam oracle maximum parameter error is zero for all four generated cases; tolerances remain frozen rather than fitted to real runs.

## Future execution and review focus

Fully implemented but not executed: native generated CUDA40, real L-only smoke32, formal42,400 and reporting. CUDA is four new cells × (warmup1+continuous2+resumed2+failure1)=24, C2/C3 old/new low-dose meter comparisons12, small Adam oracle4. Smoke is four cells ×8, seed163/O1, formal warmup with no U/val/test, all state discarded. Production real cap42,432. Existing software is required; actual fingerprint mismatches stop baseline reuse.

Please inspect the narrow adapter against old trainer, exact scalar/VJP and foreach arithmetic, restored semantic identity, canonical refusal coverage, new authorization boundaries, prefix/target acceptance, finite cost closure and historical provenance. CPU PASS is not evidence of native CUDA, real prefix acceptance, real smoke, formal performance, or external approval. CUDA/default foreach and production384 resource behavior remain pending checks.

Export is implemented for 28 final, 84 domain, 63 paired and 64 Adam records plus gradient, cost, public results and completion proof. Historical Adam remains NOT_MEASURED_HISTORICAL. New-dose G_perf/G_key are fixed development thresholds; only the same lambda can pass both, tie selects0.5. If both new doses fail performance, close this dose study and current NKA-SWD candidate. No automatic subsequent experiment follows any outcome. Independent-patient generalization, complete new-method trajectories, original KI reproduction, SOTA and high-dose C1 superiority are not supported.
