# Frozen SIGN replication V0.1

A and B are jointly frozen by PROTOCOL and SOURCE_FREEZE before any new score analysis or training. A is post-hoc old-score/log analysis, not an admission test. B has exactly new optimization seeds71/72/73, split0, two orders, six freshly initialized SRC_CE and twelve paired target trainings. The previous main terminal remains NO_PRIMARY_ACCURACY_GAIN. No old source/target weights are loaded.

Source recipe is the unchanged AMS_SEQ_TRANSFER SRC_CE engine and SUP_CE math, originally executed at f174cf2a68212cf04ad19b18d8203a6455c94aee. Source100 epochs have3200 RIM or2100 Drishti steps. New source final students only feed their own seed/order targets; target EMA and Adam restart. Source validation covers only its own domain and cannot select weights.

SIGN calls the frozen dpr_finite_v0_2.core.step directly. Its file hash binds exact executed math, FP32/FP64 paths, geometry,2 probes,32x32 pool,2x2 solve, guard and DPR_FINITE_V0_2/sign_control keys. F_CONV calls native T_LCTX with no U loader or VJP; a task-scoped loader schedule disables response access but does not alter native epoch/LCTX math. Both use the same fourteen438192 trainable scalars. Full completed F_CONV warmup records precede SIGN, whose student/EMA/Adam/order hashes must match at epoch20.

Reuse the original SRC_CE loop and common target loop; do not copy or modify inherited math/loops. Target final-adopted pending checkpoints precede diagnostics, which must pass on resume. Raw/candidate failures are forensic-only. Source dynamic recovery follows the original epoch-boundary source contract; epoch20/100 snapshots are retained. No automatic retry. Operation telemetry includes failed attempts independently of scientific status.

A reads saved paired scores only, using shared physical-domain draws2000, seed2026091301, SIGN-F_CONV and SIGN-DPR_U. Existing per-step logs provide joint accepted*algebra_eta/d0, b/Rnorm/delta/eta and acceptance statistics. The unavailable full-vector actual FP32 correction norm is NOT_RECORDED, not imputed. Missing noncritical diagnostics cannot veto B. Diagnostic positions are all inherited positions, never selected by results. q_sign cancellation and Rnorm changes mean the control is not strictly correction-amplitude matched.

B's only primary is new SIGN-new F_CONV, orders averaged within each optimization seed then seeds averaged.2000 paired patient draws use2026091302. Positive mean gives POSITIVE_REPLICATION_ESTIMATE; otherwise NO_POSITIVE_REPLICATION_ESTIMATE.0.005 is a separate practical reference; patient interval, order/seed variation and new/old/class costs are separate. Reused development patients are not independent confirmation. All12 final target weights seal before target evaluation.

Normal formal optimizer/backward/EMA47700, source15900 plus F_CONV15900 plus SIGN15900. Nondegenerate SIGN25440 VJPs and38160 response forwards. Real-L smoke cap8; synthetic integration cap64 including failures. Qualifications reuse established math certification and only test new source lineage, baseline access, exact warmup, kernel identity, final-pending recovery and native deployment. GPU4/5/6/7 require free-memory headroom; no exclusivity or unrelated-process changes.

Archive sources, final students, recovery files and private scores on NAS. Public output contains only code, frozen configuration, aggregate tables, cost and provenance. A remains separate from B; no retrospective replacement of old primary. Finish the fixed matrix, publish and stop, with no new tuning, seeds, methods, test/hidden GT or main merge.
