# DPR finite response V0.2 frozen execution contract

Base report commit69fba7c9d28dd4fb572b220d6ec5a6ffdb7089b9; historical DPR execution12ba9232204d5f183f48b67261a91efc0e4f6392. Existing terminal SMALL_POSITIVE_PRIMARY_SIGNAL is retained. This branch adds only two new arms and does not change inherited files.

The machine-readable PROTOCOL fixes12 tasks,31,800 updates, the6 original SRC_CE hashes, GPU6/7, the original14-layer F_CONV whitelist, LCTX/Adam/EMA, seeds, splits, thresholds, diagnostic positions and analysis seed. Baseline binding includes original F_CONV warmup model/Adam/EMA/order hashes for zero-retraining comparison at epoch20.

At each active update, supervised g precedes a no-grad current-U before response. Native Adam steps once. At raw parameters, the same U yields realized Delta, detached two-coordinate probes normalized by one global Delta Frobenius norm, q and two VJPs R. Both J and b divide by Rnorm. The minimum correction eta solves the single2x2 system with g^T eta=0. The one FP32 candidate is actually evaluated on the same U and accepted iff its entire geometry-only pooled response energy is no larger than raw. Otherwise saved FP32 raw weights are restored exactly; moments stay advanced. No search, interpolation or skipped native update. EMA follows the final adopted state only.

Finite candidate algebra, FP32 cast, candidate/applied progress and guard checks are saved before acceptance diagnostics can abort. Algebra tolerance is1e-12 times the relevant product/norm, not a relaxed absolute1e-4. Independent FP32 rounding envelopes use adjacent representable half-spacing plus FP64 addition allowance. Degenerate g/Delta/R<=1e-30 explicitly keep raw; a single zero coordinate is legal. Nonfinite inputs/forwards/solve/weights are engineering failures.

The frozen V0.1 common loop is reused without modifying it. PRE_UPDATE_RECOVERABLE is saved first. Its historical POST_CORRECTION_PENDING_DIAGNOSTICS spelling means final-adopted model, Adam, EMA, RNG, ordering and counters, never a raw/candidate intermediate. Pending diagnostics must finish on resume. Raw/candidate failures produce FORENSIC_ONLY_NOT_RESUMABLE evidence with explicit phase and actual operation telemetry. No automatic retries. Only current parameter-vector snapshots required for pending diagnostics are retained; no images, response fields, VJPs or historical response directions are serialized.

The42 pre-specified diagnostic positions separately report A sampled-linear b+Jeta, B same-detached-probe finite residual, C full pooled field guard, D native384 contrast RMS/prediction change, and E identical-current-LCTX losses. Candidates and final adopted outcomes remain separate. A/B/C/D have different estimands. C nonincrease is imposed by design and cannot establish independent retention. LCTX diagnostic forwards are additional cost; no extra backward or GT scope.

Qualification is bounded to96 cumulative synthetic optimizer updates, including failed attempts, and8 discarded real-L-only smoke updates. Source/baseline retraining0. Expected nondegenerate formal counts:31,800 supervised backwards/updates,50,880 response VJPs,76,320 response forwards,49,920 current-U accesses,63,600 L records. Actual counters govern reporting; legitimate degeneracies can reduce query/VJP counts. Qualification and diagnostic/evaluation forwards are separate. Fixed final weights seal before isolated seen-domain validation. Final public results exclude patient identifiers, per-case scores, data, checkpoints and private paths.
