# User-authorized GPU scope update

The user expanded shared execution to physical GPUs **4, 5, 6, 7**, even with existing processes present. No kill, preemption or reservation is authorized. This update supersedes the earlier three-GPU resource restriction for later phases without changing any scientific gate or budget.

The already-tested B0 executable and planned assignments remain unchanged: seed0 on 5, then seed1 on 6 and seed2 on 7 only after seed0 engineering and archive admission. This subset uses all three authorized B0 seed slots without a needless training-code change. GPU4 joins subsequent K2/validation/integration/formal scheduling under each phase’s separate admission. Earlier audit/registration bytes remain preserved.
