# Regenerated B0 v3 final report

PASS_ALL_THREE_REGENERATED_B0_SEEDS. Three seeds, three domains and 100 epochs/domain are complete; all three server-local parent exit receipts contain actual child exit 0. Each seed has exactly 5,295 contiguous steps, three independently loadable stage-best checkpoints, complete deterministic evaluation matrices, no nonfinite training evidence and direct historically captured PAS bank/count/mask hashes.

The evidence adapter leaves the repaired Gate0 training equations, selection, augmentation, optimizer/scheduler, EMA and GAS unchanged. No PAS reconstruction, C0 or method training was performed. The original evaluator-only test GT contract was retained to reproduce the required stage matrices: no hidden or test GT enters training or checkpoint selection. Gate1C test GT remains forbidden.

All three private bundles were remotely verified, copied to fresh private local incoming directories, verified byte-for-byte and atomically promoted. Remote copies remain. Total: 177 manifest-listed files and 104858392 bytes. Manifest files themselves are additional.

Descriptive comparison: NO_REGEN_BASELINE_DRIFT_FLAG. New three-seed final foreground Dice mean: 0.6178326632221514; old public mean: 0.6178326632221514. Difference: 0.0 pp. All recorded stage-matrix values equal the old public values: True. No result-driven tuning.

B0 exact source: `4e3e2740e1ecd3285390a5f051c439a044fe3d9a`; independent execution authorization: `bedf54fedf8bd01f8c8c14f53c44ee11d1f6a9c4`. Seed0 ran on shared GPU5; after its engineering/exit/archive gate passed, seeds1/2 ran on shared GPU6/7. User-authorized diagnostic scope is GPU4/5/6/7; no existing GPU process was stopped.

Warnings: original scheduler order/epoch deprecations and CUDA cross-entropy deterministic-kernel warning are preserved. GPU runtime trajectory/resume tests passed under the original warning-only policy. The first test-launcher old-host path failure and a later pre-training GitHub fetch timeout are preserved separately; neither is a completed or failed B0 training seed. The published Git bundle fallback transferred the exact authorized commits over SSH without changing network or access settings.

Next gate: prospective B0-EMA-only K1/K2 replication. No Gate1C cache-generation forward has run. Overall Gate1 remains FAIL_TRANSPORT_NOT_SUPPORTED.
