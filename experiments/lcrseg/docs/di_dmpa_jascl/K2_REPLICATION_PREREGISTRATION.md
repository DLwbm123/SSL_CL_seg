# B0-EMA K2 replication preregistration

Prospective; no real K2 feature forward has run. Only regenerated B0-EMA is evaluated, with K1 reference and K2 candidate. This is not a new K search. The three independently verified/archived B0 seeds are bound by the aggregate manifest SHA in the JSON.

Reuse the exact public coordinate plan and five registered bootstrap case draws, frozen null-aware sphere geometry, class/case weighting, five clustering restarts and Hungarian matching. Preserve every UID and null row; use R95_null_worst_case only. Fit train_labeled, evaluate val, and exclude background from admission.

All six conditions must pass: at least 12/18 foreground radii improve; the median of nine seed-domain foreground-macro relative reductions is at least 10%; at least 90% of active clusters have occupancy at least 5%; matched bootstrap cosine median is at least 0.85; at least 2/3 domains improve; background is excluded. Thresholds and formulas are unchanged.

Exactly 18 feature units and 54 class/K geometry units; expected 93 batched model forwards. These belong to K2 replication, separate from the later 2865 Gate1C validation/integration/formal forwards. No C0, student panel, K3, K5, learned transport, reconstruction, hidden/test GT, optimizer, training or model update.

Use shared physical GPU4, within the authorized GPU4/5/6/7 scope; retain every existing process. Exact source 9a33734d3efdb13d93b6260b4476e2c9a3f32831 passed 56 GPU runtime checks with one disclosed old-host duplicate skip, and 33 separate CPU geometry checks without skips.

PASS retains selected_K=2 and permits a subsequent separate Gate1C preregistration. FAIL stops before Gate1C. Overall Gate1 remains FAIL_TRANSPORT_NOT_SUPPORTED.
