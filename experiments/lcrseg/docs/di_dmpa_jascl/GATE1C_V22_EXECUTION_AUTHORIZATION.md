# Gate1C v2.2 conditional execution authorization

Recorded 2026-08-31T01:04:36Z. **CONDITIONAL_EXECUTION_NOT_LAUNCH_READY**.

This is the independent operational authorization required by the published
[v2.2 execution preregistration](DI_DMPA_GATE1C_V22_EXECUTION_PREREGISTRATION.md),
commit `9593908bd36f7f833e385a70b2b772b7a8c84d22`. It applies only to the
current JASCL / DI-DMPA offline diagnostic under the user's existing
[current-method-only authority](LONG_RUNNING_REPRODUCTION_20260830.md).
The [JSON](GATE1C_V22_EXECUTION_AUTHORIZATION.json) is the binding record.
It is not a new scientific amendment or permission to train.

## Recovered access and bounded evidence

The same notebook hostname and preserved volume are reachable. The observed
process generation began at remote local time 2026-08-31 08:53:55; the actor
and cause are unknown, and no restart was performed by this agent. No
experiment process or CUDA compute process was running. Historical worker exit
codes cannot be recovered from the current process table; the original failure
status, tracebacks and files remain available and were hashed.

The [live receipt](gate1c_v22_preflight/LIVE_RECOVERY_PREFLIGHT.json) verified
all 648 indexed old-v2.1 files (5,243,626,652 bytes), the 65 indexed pilot files
plus its manifest (66 files / 122,825,857 bytes), nine B0 checkpoints, all 27
original K=2 geometry records, recovered legacy PAS file/proof, manifests/splits,
official classifier and all three protected numerical engine files.
No checkpoint tensor or cache array was loaded; no forward or update ran.
The 495-cache numerical/provenance reuse gate remains unapproved.

The first one-off checker stopped on an overly broad upstream-clean assertion:
seven existing untracked `__pycache__/*.pyc` files. Its exact script and error are
preserved in the receipt. The correction uses the already tested pilot's
tracked-source guard, verifies the classifier hash and additionally enumerates
and hashes all untracked files, rejecting non-bytecode additions. No source,
artifact, bytecode or scientific requirement was altered or deleted.

Observed root free space was 9,225,650,176 bytes; both original 3090s were idle.
Local archive headroom and the existing ignore rule passed. These observations
are point-in-time and must be refreshed before execution. Remote wall time was
about 130 seconds ahead of local time; runtime budgets use monotonic elapsed time.

## Strict execution order

1. Publish this authorization, then minimally extend and publish the existing
   shared runner. Keep the tested numeric engines and protected science intact.
2. Run all existing and new synthetic checks on a clean checkout of the exact
   published code: at least 138 tests, zero failures/errors/skips.
3. Only then load input payloads and all 495 caches for exact-code provenance,
   shape/dtype/null/finite/identity audits, still with zero forwards.
4. Only after those audits pass, perform the new three-pair/four-phase real
   integration, 75 forwards. Old pilot rows cannot substitute for this evidence.
5. Only if integration and refreshed guards pass, execute one formal 72-pair,
   four-phase attempt: 36 pairs per GPU, 1,800 forwards, full phase barriers and
   C1-C8 compilation. Combined new forward cap remains 1,875.
6. Independently verify the complete artifacts, referenced files and private
   local archive, then publish only scrubbed evidence and the actual conclusion.

All original data roles, seeds, probabilities/PAS, FP64 same-Gaussian VJPs,
thresholds, budgets, create-only paths and preservation rules are unchanged.
Every prerequisite fails closed. No training, registration, additional method,
Gate2, resource purchase, environment change, artifact deletion or main merge.
Neither this record nor engineering success overturns Gate1B or old Gate1C.
