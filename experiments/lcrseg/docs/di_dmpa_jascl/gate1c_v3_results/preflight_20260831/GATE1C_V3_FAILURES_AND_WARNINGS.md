# Failures and warnings

- Blocking: candidate destination SSH authentication returned
  `Permission denied (publickey,password)`; hostname/uid and writable roots
  remain unresolved. Local SSH exit 255 is not experiment evidence.
- The name `jiangsuiyang` is not a usable configured OpenSSH alias in this
  local environment. Repository endpoint and historical paths are candidates.
- Local checksum/JASCL success does not establish server readiness.
- The local reference has pre-existing untracked metadata and Python bytecode;
  tracked source matches the required official commit. Nothing was cleaned.
- Current checkpoint serialization is not yet sufficient for v3's direct
  historical PAS provenance. No training or checkpoint-selection repair was run.
- No v3 supervisor, disconnect, process-exit, archive-promotion, numerical,
  integration or formal test ran. Historical 210/210 and 75-forward PASS remain
  historical evidence only.
- The local preflight bundle is not a backup of unavailable v2.2 references.
  No remote experiment copy or verified B0/cache/array archive exists.
- Initial static source lookup used three guessed filenames that did not exist
  (rg exit 2); paths were located with rg --files before further inspection.
  This was a read-only lookup error, not an experiment attempt.
- Data inspection used the original read-only verify_checksums function directly.
  The existing verify_local_bundle CLI was deliberately not executed because it
  writes a validation report under the frozen data root.
- No failed or unobserved criterion was converted into a scientific outcome.
