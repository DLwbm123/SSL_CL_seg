# Pre-forward v3 serialization admission failure

The actual server-local child exit was 1. Zero workers, model loads, cache files and Gate1C forwards occurred; integration has never started. This is an engineering registration failure, not a scientific result or an SSH failure.

The historical H helper preserves dictionary-key order, while the new create-only JSON writer sorts keys. The original preregistration builder hashed the in-memory pair dictionaries before serialization, creating a self-inconsistent digest after publication. All 72 pair values, ordering, seeds and checkpoint SHA values were identical. The original guard correctly rejected this mismatch.

The failed output, parent receipt, logs and manifest are privately archived and retained remotely. A new prospective append-only erratum is needed before any actual cache generation; the original preregistration and authorization are not modified. The correction must pass a serialize/reload/full-contract check and preserve every numerical equation, threshold, pair value and forward budget. No integration retry is authorized.
