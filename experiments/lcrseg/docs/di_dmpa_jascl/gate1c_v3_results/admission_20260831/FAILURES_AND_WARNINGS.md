# Admission development failures and warnings

- Initial public-key-only access failed; the earlier blocked preflight remains unchanged. User later supplied the actual SSH credentials.
- The first authenticated SSH master could not create its overlong local Unix socket path. A short private temporary socket path resolved the transport-only problem. No experiment ran during either transport problem; neither SSH return code is scientific evidence.
- First durable CLI development check failed because argparse consumed options as command arguments; fixed before infrastructure source publication and verified by the real detached-parent test. Its original local failure evidence remains private.
- The initial prototype-count unit assertion incorrectly assumed translation augmentation had no ignore-label border. The actual capture was 661 valid pixels, not 768. The test now independently counts the exact original augmented prototype batches. No training equation or augmentation was changed. Failed JUnit is preserved as v3_local_03.xml.
- The unchanged old scheduler emits its existing ordering/deprecation warnings. Its ordering is intentionally preserved for the frozen training semantics.
- The first local original Gate0 suite has 48 passes and four old-host `/root/LCRSeg` skips. Remote checks will point the three read-only manifest tests at the audited path; the duplicate six-step TinySegNet old-path test remains explicitly disclosed, while four production-model synthetic resume trajectories and the new full three-stage adapter/audit test execute.
- The intentional server child exit 7 is a supervisor test with preserved truthful parent receipts. It is not a failed experiment.
- No formal B0 run or Gate1C diagnostic forward has started at this admission milestone.
