# B0 v3 execution authorization

Authorized within the user’s explicit clean-regeneration instruction, after separately published preregistration `91d4533` and runtime-only amendment `53d98cf`. This is not a claim of an additional independent/user review. Publish this authorization before launching formal B0.

Exact execution source: `4e3e2740e1ecd3285390a5f051c439a044fe3d9a`. The 54 applicable runtime tests pass, one preregistered duplicate old-host test is skipped, the parent records actual child exit 0, the remote phase manifest is complete, and the full private runtime bundle is independently byte/SHA verified and atomically archived locally. Evidence hashes are recorded in JSON; original failed test evidence remains preserved.

Start B0 seed0 on shared physical GPU 5. Only after its completed three stages, independent full checkpoint loads and matrices, leakage checks, actual parent exit 0, and verified private local archive may B0 seed1/seed2 start on allowed GPUs 5/6/7. Each execution uses a fresh create-only output and the server-local detached parent. Recheck frozen data and free storage. No process kills, preemption, reservation, permission changes, packages, old-output deletion or old-v2.2 retry.

This document authorizes no C0, learned transport, method training or Gate1C forward. The current phase must emit its own exact commands, metadata, checkpoint/PAS hashes, stage matrices, completion/exit receipts and private archive audit. A scientific result cannot be inferred from an SSH status or an unverified artifact.
