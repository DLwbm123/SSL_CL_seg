# Gate1C v3 publication receipt

Report commit: `b38bc524bc9c653cc373290d166639c02375b10d` on `codex/gate1c-v3-clean-regeneration`; `git ls-remote` verified the identical remote SHA at 2026-08-31T11:37:29.074443+00:00. The JSON companion binds every required report file to its SHA-256 and retains exact Git argv, exit statuses and command-output hashes.

Scientific result: **FAIL_IDENTITY_HISTORY_RELIABILITY_NOT_SUPPORTED**. Reduced candidate: **NONE**; historical-bank claim allowed: **false**. R3 pixel fails C3/C4/C6; class-balanced R3 fails C3/C4/C5/C6; pixel-normalized R2 also fails admission. Overall Gate1 remains **FAIL_TRANSPORT_NOT_SUPPORTED**. The prototype-reliability line is stopped.

All **990+75+1800=2865** new Gate1C forwards completed, with 72 formal pairs and 576 teacher-draw records. Completed validation, integration and formal parent exits are 0. B0 training, K2 feature forwards and engineering tests are separate from this diagnostic budget. The earlier zero-forward serialization-admission failure remains preserved with actual exit 1 and its separate prospective correction; no second integration was run.

Complete private archive: **14,470 files / 17,712,127,650 logical bytes**. Content SHA-256 `8a82c7b8f0c72eb4faf619f51d7c1eae67a5f81059bc7f283b6b8df22d563526`; manifest SHA-256 `480b627e0f63839ff5430d980020ca026c45838cf5eeb345f2b4cf7c4d578bb2`. Remote and local bytes were fully verified, local promotion was atomic, and the remote copy was retained. Raw models/arrays remain private.

The initial publication whitespace check exited 2 because the unchanged CSV writer produced CRLF lines. All 73,952 flags in eight CSV files were verified as original CRLF only; every file still matches the private archive byte-for-byte. The command-scoped CRLF-aware check passed, with normal trailing-space/blank-line checks retained and no persistent Git setting changed. A subsequent staged-byte audit identified two original failed-admission logs excluded by the existing `*.log` rule; only those two SHA-verified logs were explicitly staged. No artifact or metadata bytes were rewritten. The JSON receipt retains these checks and hashes of their private raw journals.

The report preserves all historical statuses and earlier failures/warnings. Old v2.2 full remains unknown, old private references permanently unavailable, and old C1-C8 null. Original handoff files are unchanged. Exact execution source is `db4af88eca0dca48025f8884bf7f85e068eabf2a`, separate from the report commit above.

This receipt's own commit is intentionally not self-embedded. After its separate commit/push, the final branch SHA is checked once more. **Hard stop for independent review**: no C0, reduced-method implementation, method training, learned transport, Gate2, Prostate/MnMS, full sweep or main merge.
