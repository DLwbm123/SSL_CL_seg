# Gate1C v3 clean-regeneration preregistration

Protocol: `DI_DMPA_GATE1C_V3_CLEAN_REGEN_K2_IDENTITY_HISTORY`. Registered at 2026-08-31T09:47:40.865832+00:00. Exact code `90b9d87bb6b8d4d40850f6c05d641d519b8d8161`. This is a new v3 experiment, not an old v2.2 retry. Execution remains unauthorized until the separate authorization is published.

All three regenerated B0 seeds and their verified private archives passed; the nine new stage-best checkpoints and directly captured historical PAS banks are bound by `e6d3c833865a3c6773d28851c11e7da48b6496661eac34e34478ff01714a6f1d`. K2 replication passed and its freeze is `f39d764832d7b88087fef9a25a618752ec2a8d0167efe24c55da16a4cfde5a7b`. Only new B0-EMA K1/K2 was replicated; there is no new K selection. Historical Gate1B and overall Gate1 remain **FAIL_TRANSPORT_NOT_SUPPORTED**.

EMA provides probabilities/features, student receives diagnostic gradients, K=2, history transform is identity, and R4 is unavailable. Pixel-normalized R1 is the fixed reference; pixel-normalized R3 is primary and class-balanced R3 is the sole independent fallback. All R0/R1/R2/R3, PoE, null, C1-C8, 72 pair IDs and forward seeds are inherited unchanged. Only checkpoint identities and operational roots are rebound. R1 reads the directly stored, historically hashed PAS bank; reconstruction is forbidden.

The unchanged native FP32 forward/scoring path and same-Gaussian FP64 shadow differentiation engine are used. The source hashes, original formulas and precision comparability thresholds are in the JSON. Every required global native/shadow gradient comparison must have relative L2 <= 0.001 and cosine >= 0.9999, with the unchanged both-zero/one-zero rules; decomposition tolerances remain atol=1e-6, rtol=1e-4. All native and shadow, model/bank/RNG, PAS parity, no-backward/no-optimizer/no-grad-write guards remain mandatory. There is no numeric fallback or favorable-row selection.

Sequence and exact new forward budget:

1. Fresh 495 validation cases, **990** native forwards and nine complete model guards. Save original scoring fields plus raw logits/probabilities/EMA and student features, PAS intermediates, null mask and full UID order, internal/outer hashes and all provenance. Extra capture forwards=0. No integration until all 495, the parent exit and private archive pass.
2. Exactly one create-only integration using the three original fixed pairs and four phases: **75** new forwards (51 native/24 shadow), 12 guards, 276 native and 366 shadow autograd calls. It generates new v3 goldens. No old private hashes and no automatic second attempt. Failure stops this line.
3. Only after integration and local archive PASS: nine reliability evaluators, 72 draw0 pairs, 576 teacher draw records, 72 posterior-mean controls and complete offline PoE controls. **1800** new formal forwards (1224 native/576 shadow), 288 new probe guards. The additional nine PoE evaluator units read the same newly generated v3 validation caches and perform no model forwards. Total new Gate1C budget **2865**.

Use physical GPUs 4,5,6,7, including when other processes remain; never stop or reserve them. Integration maps its three fixed pairs to 4,5,6; validation/formal shard deterministically across 4–7. Use the existing Python 3.10.6/PyTorch 2.2.1+cu121 environment without installing packages. Server-local parents own the children and publish actual exit codes; SSH return codes have no experimental meaning. Commands, closed logs, phase manifests, exceptions and all warnings are retained. There is a fixed six-hour maximum per worker phase and a 32 GiB remote free-space launch gate, not a result-adaptive retry policy.

The exact-code server tests and actual-model engineering checks are independently archived and hash-bound in the JSON. The one disclosed historical-root-only duplicate resume test is skipped; production-model resume trajectories execute. The old opt-in private-input integration module is excluded from the synthetic suite because those inputs are forbidden and unavailable.

Every required completed scope is first SHA-verified remotely, transferred into a fresh private local incoming directory, verified byte-for-byte, then atomically promoted to a content-addressed archive. Remote copies are retained. A final index binds all child bundles, including checkpoints/PAS, K2, fresh cache/raw values, integration/formal arrays, command/configuration and code/data hash evidence. Raw arrays/checkpoint bytes are private.

Only the three registered scientific outcomes are legal. R0/R2/PoE/posterior controls and teacher-draw selection cannot rescue R3. For the separate future-candidate decision, independently passing pixel-normalized R2 can nominate CURRENT_ONLY_MPA_JASCL after R3 fails; this does not change the R3 failure and forbids a historical-bank claim. Class-balanced R2 is descriptive only. Passing R3 nominates HVR_MPA_JASCL or CB_HVR_MPA_JASCL as specified; both R2/R3 failing yields NONE. No reduced method is implemented here. Any eligible future candidate needs separately preregistered C0 seeds0/1/2 before performance comparison.

After the complete report and archive audit: commit, push the v3 branch, verify the remote SHA, and **stop for independent review**. No C0, method training, Gate2, Prostate, MnMS, sweep or main merge. Never claim DI-DMPA reproduced or PASS_CORE_ADMISSION.
