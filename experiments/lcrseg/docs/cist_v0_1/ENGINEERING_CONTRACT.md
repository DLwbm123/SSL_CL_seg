# CIST V0.1 frozen implementation contract

The attached plan is implemented in an isolated namespace from f54e793. No historical code, lock, terminal or score is changed. The six original AMS sources and18 cached evaluations are bound before new data access. Full sources are never retrained.

The explicit appearance formula is interpreted literally: one gamma per image, three independent gains and three independent Gaussian channel offsets, each constant across spatial coordinates. There is no crop, flip, rotation, interpolation, GT-derived valid mask or pseudo label in the U branch. Geometry masks are inherited image support only. The conditioner uses population variance. The random coordinate arm changes coordinates, context and the view-loss coordinates together. All non-SCALE arms have2920 raw parameters; GLOBAL input weights have zero data gradients, so this does not imply equal effective degrees of freedom.

The shared source core uses the original384 image path, raw linear3x3 readout (padding0), and bilinear output interpolation with align_corners=True. Original source normalization remains in eval mode. No EMA or second full model is created. The constructor's trainable module ends at exactly zero output. The actual production transport is FP32 H plus FP32 V times the update from the FP64 differentiable Cayley Q cast to FP32. Diagnostics measure the actual result, with a source-distance denominator and explicit near-zero source-energy floor, never the update norm. FP64 orthogonality limit1e-10 and FP32 distance relative limit2e-5 are fixed engineering checks, not accuracy gates.

Before each optimizer step, pre_step.pt atomically stores the exact controller, Adam, RNG, data position/order and counters. The frozen full core is referenced by the bound source student hash, and bases by their hashes. Every solve atomically journals PRE_SOLVE M and POST_SOLVE Q against that checkpoint. Recovery replays the whole uncommitted step; no claim is made to resume a serialized autograd graph. After the optimizer, latest.pt is saved before pending diagnostics. Pending states must pass diagnosis before another optimizer update. Epoch20/100 exact controller checkpoints are retained. The current solve journal is removed after the committed step; it is not a patient Q/b cache. Interrupted/failing states retain their journal and checkpoint as engineering evidence.

The fixed matrix is36 new targets and95400 optimizer steps. Qualification allows at most128 synthetic steps total, with all failures counted, plus a separate fixed24 real smoke steps (six arms, two domains, two updates each). Smoke controllers are discarded and source files remain unchanged. The planned complete synthetic regression is26 steps on each backend. Testing covers identity, gradient/orthogonality, all six arms, source isolation, L-only U denial, two-case overfit and injected solve/pending/save interruptions with exact replay. No validation metric admits or removes tasks.

The analysis seed2026091011 and2000 paired bootstrap draws reuse the existing local reporting convention. All36 final weights are sealed before full-matrix analysis. The primary criterion is the mean Final difference versus F_CONV, with0.005 practical signal; positive smaller values are a small signal. Domain/class tails, absolute Forget, seed costs, Pareto and component ablations are mandatory disclosures and do not form conjunctive gates. CI crossing zero indicates uncertainty. Repeated development patients do not establish independent confirmation.

Workers use the NAS wrapper and neutral visible argv. A GPU with existing processes can be admitted only when measured smoke peak times1.25 plus512MiB (at least1536MiB) is free. Workers wait if memory is insufficient; tasks are never reassigned based on scores. One task trains then evaluates per worker. The queue stops new dispatch on an engineering failure, preserves artifacts, and never auto-retries failed training. Successful completion seals and summarizes the entire fixed matrix, then exits without extra science.
