# R1 response — code-only repair, awaiting independent re-review

The external decision for `4ac9a27b52a9d381dce6bdfaf96bba23b17245f8` is **CHANGES_REQUESTED** and remains unchanged. Original inputs are preserved verbatim in [external_review_R1](external_review_R1/EXTERNAL_REVIEW_R1.md). No approval or launch confirmation was generated. State: **STOP_AWAITING_EXTERNAL_CODE_REVIEW**.

| Finding | Repair | CPU evidence | Production evidence |
|---|---|---|---|
| R1 sealed model integrity | [assurance.py](../../f5_confirmation_v1/assurance.py): verify_file, integrity_current. [execution.py](../../f5_confirmation_v1/execution.py): METADATA_SEALED and permit-bound accept_target for all 28 targets, including skips and terminal stages. Actual file/tensor hashes, schema, finite state, node/identity/step, F checks, file freshness and receipt/code/plan binding. | integrity_faults rejects truncated files, wrong weight/F/identity/schema/dtype/shape, NaN, missing declared hash; genuine generated tensor file verifies idempotently; changed file invalidates evidence. report_artifacts confirms a fake nonempty student cannot produce completion. Existing lost-tail and new ledger-gap tests remain. | All real target/source tensor checks PENDING. No real tensor was read in this repair. |
| R2 native qualification | [native_qualification.py](../../f5_confirmation_v1/native_qualification.py): 12 explicit cases, 21 planned physical calls (including three deliberate failed updates), bounded by 60. Uses actual NativeLRParent, native_state.resume, checkpoint and StageTrainer; full merge/continuation/stage-two checks. Qualification-only synthetic support fixture and nonzero SWD check. [execution.py](../../f5_confirmation_v1/execution.py): exact case/check/range/count/environment admission. | qualification_coverage rejects empty/missing/failed/miscounted cases, environment mismatch and ledger gaps. Tests do not construct a production permit. | Native CUDA remains PENDING; case definitions are not a CUDA PASS. Actual runtime fingerprint is frozen at first authorized CUDA invocation and matched at smoke/run; absent historical GPU/cuDNN details UNVERIFIED. |
| R3 cost collection | [assurance.py](../../f5_confirmation_v1/assurance.py): cost_session/session_totals reuse NativeOperations with durable optimizer-boundary snapshots, per-session wall time and CUDA peaks. execution wraps targets, qualifications, smoke and integrity/source checks independently. Failed/earlier sessions remain; hash/load/forward work is separately recorded. | cumulative_costs_rng checks collection on/off leaves outputs, gradients and RNG unchanged; verifies matching backward counts, accumulated two-session costs and failed-session retention; unclosed hard-kill evidence refuses continuation. Zero optimizer calls in this new test. | Actual CUDA peaks/operations PENDING. No fabricated values or NA substitution for future collectible counters. Hard-kill tail cost cannot be reconstructed: retained lower bounds plus explicit engineering stop, not a silently successful recovery. |
| R4 report delivery | [reporting.py](../../f5_confirmation_v1/reporting.py): six actual artifacts, 18 final trajectories, 90 stage/domain rows, 34 paired-summary rows, explicit origin, separate costs/integrity. Gate math unchanged. Completion only after all evidence and artifact schemas; publication status separate. | report_artifacts renders pure generated aggregates, validates origins/coverage/metrics/gates/schemas, rejects malformed output, incomplete matrix and missing integrity. Renderer fixtures are not production completion evidence. | No real P1 result/report exists yet. Source/CUDA/smoke/formal completion all PENDING. |

## Scope and protocol binding

[PROTOCOL_DIFF_R1.json](PROTOCOL_DIFF_R1.json) compares against the preserved PLAN_PRE_R1.json. Changes are limited to CPU regression case names, explicit native qualification cases/options/environment binding, integrity schema, report schema and resulting hash. Parent, scientific nodes/options, random streams, source reuse, imports, statistical definitions and gates are unchanged. The original five-framework implementation is untouched.

The CUDA fixture uses generated images/labels and an explicitly qualification-only foreground-biased random native readout. PAS 0/-1 and four-step warmup/ramp options are scoped to qualification; the scientific options stay frozen. Continuation branches and deliberately failed optimizer attempts are all charged to the 21-call qualification plan. Additional synthetic predictions/loss evaluations are measured by NativeOperations, not counted as optimizer calls. Current-L smoke remains 24; formal remains 74200; no source training or P2 execution is added.

## CPU evidence and costs

This repair added **10 CPU synthetic optimizer calls** across two bounded suite invocations. The final suite has **13/13 PASS**; cumulative cost is **25/40 calls and 5/8 invocations**. The original 15/40 optimizer calls and 3/8 invocation attempts remain in CPU_PHYSICAL.jsonl and CPU_ATTEMPTS.json. See the current TEST_REPORT.json for cumulative totals and current code/plan digests. New regression groups use generated tensor files or metadata only and add zero optimizer calls themselves; each full suite rerun still executes the existing five optimizer calls, including its deliberate failed update. TEST_REPORT_PRE_R1.json preserves the old submission; all original failed attempts remain in the cumulative history.

Real patient payload reads = 0; real checkpoint tensor reads = 0; native CUDA execution = 0; real smoke = 0; formal optimizer updates = 0; monitoring started = 0. No test PASS is an external approval. Push this revision for independent re-review and stop.
