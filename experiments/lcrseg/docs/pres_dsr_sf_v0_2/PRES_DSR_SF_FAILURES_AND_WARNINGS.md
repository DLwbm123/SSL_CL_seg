# PRES-DSR-SF V0.2 failures and warnings

- Protocol status: `BLOCKED_INCOMPLETE_EVIDENCE`.
- Scientific status: `NOT_ADJUDICATED`; E1-E6 are unavailable.
- Root cause: six routers times `(5 lambda rows + 4 temperature rows)` equals 54, while the frozen coverage assertion incorrectly required 78.
- The error was detected before router sealing, expert-probability inference, validation segmentation evaluation, and bootstrap evaluation.
- Existing input, descriptor, memory, process, and failure evidence is preserved in the sealed private archive.
- This engineering blocker is not evidence that the learned-soft router passed or failed its scientific gates.
- No additional attempt is authorized without a new prospective protocol and independent authorization.
- Public artifacts omit private paths, case identifiers, descriptors, probabilities, masks, checkpoints, and raw CSV rows.
