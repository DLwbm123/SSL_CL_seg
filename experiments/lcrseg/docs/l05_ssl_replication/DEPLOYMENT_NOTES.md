# Source deployment

The server GitHub HTTPS fetch failed with GnuTLS error -110 before deployment. The published Git objects were transferred over the authorized SSH connection with git pack-objects / index-pack, then checked out by the same immutable commit. No archive/source substitution and no experiment input replacement occurred. The old worktrees remain untouched.

Qualification CLI execution requires PYTHONPATH=.:experiments/lcrseg for the original standalone scoring script sibling imports. Local and server source 53075d055cc78a10b03e6b1da4ca2b436c9a1cf7 both pass all eight scoped tests.

The background finite DAG writes terminal status and raw aggregate reports to NAS. GitHub result closeout will run when a subsequent status request confirms completion; no Codex polling or scheduled monitor was created.

The inherited low-level lifecycle log uses legacy D for both L05 and L05_SSL. Checkpoint recipe/optimization_seed/data_split_seed and final_receipt.json identify the actual new recipe; result tables map these explicit identities. Legacy D in a step log does not mean an old D experiment was rerun.
