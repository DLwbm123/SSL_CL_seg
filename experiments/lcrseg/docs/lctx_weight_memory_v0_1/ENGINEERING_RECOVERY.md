# Cold CUDA initialization recovery

Attempt01 on c097633 stopped before any formal optimizer update or image/GT read. All four dispatched trainers hit `reset_peak_memory_stats` before CUDA initialization; the warm-process qualification/smoke did not reproduce this startup condition. The parent stopped further dispatch. The original reservation, source/bases, logs and failure receipts remain untouched on NAS; public qualification and launch evidence is retained under attempt01.

The repair adds explicit CUDA initialization immediately before resetting peak statistics in the shared trainer. No objective, parameterization, basis generation, task, data, optimizer, threshold or random key changes. New exact-source cold-process engine checks each execute two synthetic updates on local CPU and server CUDA. The CUDA check requires CUDA to be uninitialized at entry. Previous complete CPU/CUDA numerical/resume qualification and 24 real-L smoke steps remain attributed to c097633; they are reused because only the pre-allocation instrumentation order changed. No real-L smoke is repeated. Total new synthetic qualification cost is118 (114 prior +4 cold checks), real smoke24, formal failed-attempt updates0, source recovery0.

A new run/reservation and regenerated weight-only source/basis binding use the recovery execution source. Its smoke-admission receipt links the original measured smoke receipt explicitly; it does not claim smoke was rerun on the recovery source. Completion accounting retains all attempts and exact-source tests. The fixed scientific matrix and95400 successful-update budget are unchanged.
