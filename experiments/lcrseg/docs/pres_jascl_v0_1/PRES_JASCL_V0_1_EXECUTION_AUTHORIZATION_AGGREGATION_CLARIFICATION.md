# PRES-JASCL V0.1 aggregation-clarification authorization

The original execution authorization at `8608353f0753a8a194799c9bc573f9f6962983c9` remains operative, now bound also to the aggregation clarification at `238fae3f5db08bdba17bf81380227e01e17ef0ba`. The clarification commit was matched by local Git, `git ls-remote`, and GitHub API, and both clarification files returned HTTP 200; their exact SHAs are recorded in the adjacent JSON.

This authorizes only the clarified, already-required regenerated-B0 evaluator arithmetic: aggregate pixel confusion within each seed/domain/policy before deriving segmentation metrics. Routing remains case-level. No threshold, candidate, seed, expert mapping, data role, input, isolation rule, output, attempt count, or hard stop is changed.

The authorization is effective only after this document and its JSON are independently committed, pushed, and remote-verified. All zero-training counters and all original prohibitions remain unchanged.
