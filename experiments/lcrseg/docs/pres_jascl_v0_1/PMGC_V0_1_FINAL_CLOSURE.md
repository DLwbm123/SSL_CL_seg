# PMGC V0.1 final closure for PRES-JASCL

PMGC V0.1 is closed with `pmgc_status = FAIL_PMGC_FEASIBILITY`. The unique base is `06945fa738cf87a6cef22db949b627490e7847f1` on `codex/pmgc-v0-1-feasibility`; GitHub API and `git ls-remote` returned the same complete SHA.

The frozen gate decision is `G1=true`, `G2=true`, `G3=true`, `G4=false`, `G5=false`, `G6=true`, and `G7=true`; failed gates are G4 and G5. The published execution contains 48 formal pairs, six integration pairs, 6654 real forwards, 684 autograd calls, and 464 passing tests. Model optimizer steps and training remain zero/false. The decisive failures remain a maximum current-class Dice drop of `0.00345029148374` and maximum K2-mode CE worsening versus K1 of `0.036544748892`.

K2 geometric multimodality remains `SUPPORTED`; K2 cross-domain functional mode value is `NOT_SUPPORTED`. The shared-parameter prototype-control line is `ENDED`. No additional PMGC, prototype-ranking, prototype-reliability, or feature-transport attempt is authorized.

No historical report, result, input, checkpoint, bank, or private bundle is modified. The next independent protocol is `PRES_JASCL_V0_1_ROUTING_FEASIBILITY`, a no-training snapshot-expert routing baseline rather than a PMGC repair.
