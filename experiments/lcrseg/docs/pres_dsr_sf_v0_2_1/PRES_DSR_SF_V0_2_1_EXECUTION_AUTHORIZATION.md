# PRES-DSR-SF V0.2.1 execution authorization

The independently published preregistration at `752e1ac7a016d619ffaa624c347fbeefa7883137` authorizes one prospective, create-only V0.2.1 validation attempt named `formal_01` after all source, test, science-freeze, remote-publication, NAS-mount, and real-write gates pass.

This authorization binds closure `de82bf94f27f91e071f9bab4e9432f1c0ee263d3`, erratum `44a8870765d1ebb5efa38843f3c20b79aeb721ec`, and registration `PRES_DSR_SF_V0_2_1_CALLGRAPH_SCOPE_RECOVERY`. It is not an additional run of the old V0.2 attempt.

The run must regenerate descriptors, memories, controls, routers, expert probabilities, candidate predictions, validation metrics, and bootstraps from frozen inputs. Old V0.2 intermediate evidence is not an input. Validation segmentation GT remains evaluator-only after candidate prediction seal.

No second V0.2.1 attempt is authorized. After the validation-only report, execution hard-stops for independent review. Test evaluation, C0 regeneration, grid expansion, validation refit, fine-tuning, adapters, other benchmarks, sweeps, performance training, and main merge remain prohibited.
