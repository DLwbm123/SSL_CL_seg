# CIST Plasticity Add-on V0.2 frozen execution contract

The user selected GPUs6/7. This overrides the attachment's broader defaultGPU list. The exact six original sources and unchanged cached CIST V/N are reused. Known CIST/GN/plasticity run directories and local worktree identities revealed only the completed V0.1 run, with no equivalent GN tasks; standard new budget18/47700 applies. No old experiment terminal changes.

All three arms share one L-only training loop. GN_L permits28 GN affine tensors/1408 scalars, GN_CIST_L adds2920 original controller scalars, CONV_CIST_L permits14 original F_CONV kernels/438192 scalars plus2920 controller scalars. Every other parameter and fixed basis buffer is excluded from Adam. The model constructor enumerates the precise architecture, parameter names, sizes and optimizer IDs. Freezing parameters does not remove the input gradient path: current features, context, transport and readout stay differentiable. Evaluation mode controls module behavior, not parameter gradients. See [PyTorch autograd semantics](https://docs.pytorch.org/docs/stable/notes/autograd.html).

The source values of the trainable subset are retained as CPU tensors only for descriptive drift; there is no second full model or old image forward. Checkpoints save every dynamic core and controller tensor plus Adam, RNG, data position/order and counters. The immutable source student and fixed-subset hash bind all remaining parameters and bases. Each step saves pre-update state and current solve evidence; post-update exact state precedes pending diagnostics. Epoch20 and100 snapshots remain. Pending recovery diagnoses first and cannot repeat a committed optimizer update. Deploy students contain the updated complete core and complete controller/basis state. Frozen-subset identity replaces whole-core equality.

Current-H geometric diagnostics compare a feature tensor and its transport from the same forward. Descriptive drift never triggers source-left/right constraints, accuracy gates or parameter changes. GN_L has no controller; its transport diagnostic is marked absent. Per-forward scalar observations are cleared each step; no image-specific Q/b cache is retained after commit. Evaluation's readout-only diagnostic compares current adapted H before/after transport, not original source features.

The previous tested LCTX objective, donor noise, spatial augmentation, source collection, ignore handling and floating-point loss arithmetic are reused literally. There is no U accessor, appearance transform, view objective, EMA or teacher in any formal new task. Frozen U-count metadata determines original21/32 steps; this is explicitly not U image access.

Local CPU and fresh-process server CUDA each run the bounded28-update synthetic qualification, capped32 per run and96 total including failures. It covers initial exact source predictions, all parameter sets, gradient paths, current-H geometry, dynamic core/Adam/RNG recovery for all3 arms, post-save failure/pending diagnosis, deployment reload, isolation, two-case overfit, and a native F_CONV one-step comparison with a fixed identity controller excluded from Adam. The native qualification reference has one explicitly counted EMA update; it never changes the student's gradients and is not a formal-task EMA. Real qualification permits only12 discarded current-L updates,3 arms x2 domains x2 updates, with actual new-graph peak memory. Admission is max(allocated,reserved)*1.25+768MiB. No smoke accuracy selects a setting.

All18 new training tasks finish and seal before any new validation access; the18 fixed epoch100 evaluation jobs then run. Results reuse all5 reference arms with original hashes/provenance. Bootstrap2000 with seed2026091202 shares physical-domain weights across methods/seeds/orders. Epoch logs explicitly normalize sums by steps. Primary, GN-component effect and patient uncertainty remain separate; no historical class veto, retrospective candidate switch or further experiment follows.

All runtime storage uses the existing NAS wrapper and neutral visible process argv. Public exports contain code/configuration and aggregate provenance/results; raw NAS paths, images/GT, patient IDs, per-case outputs and weights are not published. A detached runner performs the fixed queue, archives results and stops; this conversation does not create periodic monitoring.
