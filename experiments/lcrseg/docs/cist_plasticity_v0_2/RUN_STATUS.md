# CIST Plasticity Add-on V0.2 launch

RUNNING_VERIFIED at 2026-09-12T10:32:49.989455+08:00. The fixed matrix is running; no completed efficacy result is claimed.

- Branch:codex/cist-plasticity-addon-v0-2
- Frozen execution source:1df286fb8f03d2654287e2d847c4842c4027d8ab
- Starting report:4c0a0435d14762e27405f2f365e5c100a3a8bc07
- Run identifier:cist_plasticity_v0_2_20260912_01
- Detached parent PID:2550734; workers use onlyGPU6/7.
- Snapshot:1367 formal updates committed across the initial two tasks; no immediate failures and no new evaluation started.
- Fixed queue:GN_L,GN_CIST_L,CONV_CIST_L × seeds61/62/63 × two orders =18 targets /47700 formal updates. CONV_CIST_L remains the sole main candidate.

Local CPU and server CUDA passed all eight grouped checks on the exact source,28 synthetic optimizer steps each. The retained initial import-alias failure consumed0 optimizer steps and accessed no data. Total synthetic qualification56; real L smoke12 discarded updates, with U/validation-image access0. The native F_CONV qualification reference contributes one explicitly isolated EMA update on each backend; all formal tasks have no EMA or teacher. Zero-update result-contract checks also verified the three primary mean outcomes and seven registered comparisons.

The six original source student/receipt identities, six original cached readout bases and30 cached reference evaluations were verified. No source, baseline or old CIST training was repeated. The known task registry revealed no equivalent pre-existing GN experiment. Old scientific and engineering terminals remain unchanged.

Real smoke counts matched1408,4328,441112 trainable scalars forGN_L,GN_CIST_L,CONV_CIST_L respectively. Maximum allocated memory950.83MiB, reserved1026.00MiB. Admission including new-graph margin is2051MiB free. Snapshot process/GPU records confirm neutral argv; public executable paths are redacted to python. Raw evidence stays on NAS.

Logs are parent.log and logs/{task}_train.log. Dynamic checkpoints and eventual full deploy students are under tasks/. All18 final training states are sealed before the isolated epoch100 evaluation jobs. The automatic fixed runner then emits public_results/FINAL_REPORT.md and all prescribed metrics, paired effects, class costs, patient intervals, parameter changes, compute/memory and NAS archive evidence, then stops. No periodic conversation monitoring was created; the detached process survives session/SSH closure.

This public delivery includes source/configuration and aggregate qualification/startup provenance. Raw NAS paths, images/GT, patient identifiers, per-case scores, weights, dynamic recovery state and raw logs are excluded. The old launch/completion documents are historical evidence and are not overwritten. Public completion delivery follows verification when the finished run is next checked.
