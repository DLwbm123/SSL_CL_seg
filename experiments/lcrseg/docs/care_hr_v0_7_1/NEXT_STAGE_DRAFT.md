# Next-stage draft: no admission

Capacity has not been evaluated, so this report does not recommend or authorize risk-model fitting. Resolve the explicit ignore-label/all-invalid scoring contract first. A later authorized continuation must finish all A1 items, freeze and publicly verify the exact scoring/action/decision implementation and real input allowlist, pass the same source on the server, seal blind probability/route/proposal artifacts, and verify the seal before own-seed train_labeled GT access. This blocked attempt does not automatically resume.

Only if a later capacity screen supports it may a separate design consider patient-grouped nested development evaluation, lambda-conditioned targets, and risk defined on the final combined action. Outer folds must isolate new risk-head fitting, standardization, selection and calibration; previously trained segmentation experts still make this in-sample development evidence. Current, frozen Ridge-hard, SHOR, PPC and simple local fusion remain controls. No automatic training permission follows.

Patient-level exchangeability needs justification; de-duplication alone does not establish it. Pooled OOF residuals, separate gain/harm bounds, multiple-action selection and domain shift do not inherit a single split-conformal guarantee. Patient tail risk and refit stability remain untested here. Reusing historical GT for risk-head fitting could change the no-revisit task definition: zero optimizer steps does not mean zero supervised fitting. Any future confirmation needs newly isolated data and must not reuse the old formal_03 test.
