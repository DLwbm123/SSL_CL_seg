# Follow-up draft only — not launched

This run supplies no scientific capacity conclusion. Do not start a risk model, router fit, conformal calibration, segmentation training or a relaxed-space rescue.

A separately authorized engineering continuation would preserve this terminal run, reuse the inherited `di_dmpa_gate1.binding.safe_asset` binding (`DATA/h5/v1/relative`), and add a deployment-layout regression independent of the executor's implementation. It must distinguish attempted GT opens, successful label decodes and dataframe-materialized domain rows in counters. The currently frozen code is not modified.

Any future continuation must use a new source freeze, full local/server A1 evidence, a new run directory, and a new blind seal/reservation under its explicit authorization. The frozen dataset identity and case cohort must remain unchanged. Restating the already resolved scoring semantics is unnecessary. The current failure is not eligible for same-code interruption recovery: its parent has exited 1 and a terminal state exists.
