# Gate1A v2 null-aware sphere

Independent offline package under preregistration
`eaae37bbaa7546679d9e6893023afbeeef0ab5c6` and authorization
`e8f558dcc3fb6054a3f757c1295bd07ede2a002b`. Historical v1 code and artifacts
are not modified. No model/transport optimizer or training method is created.

Every registered UID has directions, explicit active_mask and raw_norms.
Finite null rows remain zero placeholders, never cosine inputs. Worst-case
null distance2 retains original weights in means and weighted ECDF. Directional
conditional metrics cannot drive A1/A2/A5. All-null fits/bootstrap draws retain
inactive slots and produce scientific lack of support, not numerical blocks.

`python -m di_dmpa_gate1_v2.runner run` checks publication/input/test identities,
copies the original sampling plan, completes72 feature units and18 checkpoint
audits, then432 geometry jobs, then B0-EMA-only admission. Controls are complete
and separate. Failed feature shards cooperatively stop after the current model
immutability guard. Every attempt and test output is append-only.

Synthetic tests include an end-to-end cache/census/geometry/report fixture.
The explicit opt-in integration retains the known seed2 registered zero in its
original batch and tests fixed null distance2 without fitting any prototypes.
All outputs are diagnostics; stop after publishing the report, regardless of
PASS or scientific failure. No Gate1B/C, training, additional benchmark or main
merge is authorized.
