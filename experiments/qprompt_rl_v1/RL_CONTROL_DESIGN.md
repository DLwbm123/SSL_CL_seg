# R3 interface only — DENY by default

Separate study `RL_CONTROL_BANDIT_V1`. A future controller would receive a frozen candidate state, a disjoint current-L fit/feedback partition, a U image region, and three actions: ignore, disc/background only, or all three classes. It would return an action and an auditable probability. Candidate student steps, anchor steps, and policy updates would each have separate physical ledgers. Behavior policy and EMA reference must be distinct. No controller code or R3 optimizer is implemented in R0; `R1_PLAN.json` keeps R3 approved=false.
