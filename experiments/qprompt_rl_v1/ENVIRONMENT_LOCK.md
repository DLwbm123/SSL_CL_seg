# R0 target environment lock — 2026-09-25

The study directory is a create-only path on a verified NFSv4 remote-home mount. Its realpath equals the requested directory. A create/write/read/fsync probe passed. The filesystem reported about 387 TB available and 1.65 billion free inodes at inspection. A per-user quota command was unavailable, so an independent quota ceiling remains unverified; the completed M1 copy and 88 MB weight acquisition demonstrate room for R0 only. No Slurm commands were installed; R0 did not reserve or use a GPU.

The target has four NVIDIA A100-PCIE-40GB GPUs and driver 560.35.03. The new study-local `env/` is a Python 3.12.7 venv with `include-system-site-packages = true`. It imports pinned observed versions from the existing read-only `/root/anaconda3` installation: PyTorch 2.6.0+cu124 (CUDA build 12.4), NumPy 1.26.4, h5py 3.11.0, SciPy 1.13.1. This environment is usable for R0 but is not a self-contained package copy; shared parent packages could drift before a later phase. No driver or shared package was changed.

For execution, prepend this venv's `bin/` to `PATH` and set `HF_HOME`, `TORCH_HOME`, `PIP_CACHE_DIR`, and `TMPDIR` to the study directory's `cache/huggingface`, `cache/torch`, `cache/pip`, and `tmp` paths. `guard_storage` checks that binding. R0 CPU checks used `CUDA_VISIBLE_DEVICES=-1`. The official DINOv2 source import closure, strict offline ViT-S/14 weight load, and both backbone inference paths passed on CPU. xFormers is absent and upstream's fallback was used. No CUDA model test or real-data optimizer call occurred.

The official weight is bound by SHA256 `b938bf1bc15cd2ec0feacfe3a1bb553fe8ea9ca46a7e1d8d00217f29aef60cd9` and stored only in the private target `assets/` directory. It is not part of the Git code tree. Precise host, user, source paths, and migration file hashes are in private receipts.
