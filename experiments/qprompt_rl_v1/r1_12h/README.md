# R1_12H_SUSTAINED_V1

Explicit user authorization starts from reviewed commit 4eac56b06cd4374d4b3c9f92ec33a070da765e3c. Historical R0 approvals, ledgers and R1_PLAN are unchanged. This dedicated executor uses only the two M1 domains, fixed seed 261, 28 tasks and 40,000 unique formal updates. Recovery attempts, CUDA synthetic calls and real labeled smoke have separate caps of 4,000 / 32 / 16. Research thresholds only annotate results.

`EXECUTION_AUTHORIZATION.json` freezes the configuration. `core.py` contains math and state primitives, `augmentation.py` ports the exact geometry and strong-transform mathematics at f6b9697 (only RNG injection changes), and `runner.py` separates preparation, training, validation and queue supervision into processes. D0 uses the existing canonical gathered pixelwise CE; no Dice term is added. Per-domain schedules fix all 3000 batch indices and geometry/photometric seeds before CUDA qualification. Augmentation is a resolved project choice, not a claim about the paper.

Every suffix restores the same complete prefix checkpoint at global step 2000. Q1–Q4 initialize a fresh bank from all unaugmented current-domain L; reference networks start from that prefix. Bank/reference/scheduler advance only after optimizer success. Failed processes restart from complete checkpoints, and repeated physical steps consume recovery budget. A task can fail while unrelated tasks continue. Source checkouts must be clean and every result binds its code commit.

Worker invocation is supplied through stdin and environment so visible process arguments are neutral. All runtime files live in a new run directory on the verified target remote-home mount. The original M1 receipt remains a data source binding, and the run produces a separate code/data binding. No private paths, medical payload, per-case schedules, raw logs or checkpoints belong in Git.

CPU formula checks in `test_contract.py` use zero optimizer calls. CUDA qualification tests both actual architectures, BF16, optimizer/scheduler/state/RNG restore, deployment equality and peak memory. Disposable L smoke initially uses two dense plus two query updates per backbone, reserving the remainder of the eight-call cap for engineering recovery. Formal jobs never load those weights. Validation reads only the prescribed final endpoint in a separate process. The final report preserves incomplete jobs and negative outcomes. R2/R3 are not authorized.
